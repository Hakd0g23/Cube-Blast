# Project State — Cube Blast

Forked from ~/Documents/VS Code/Fracture (fresh repo, no shared git history — Fracture's GitHub repo is private, standalone project per user request). GDD: `docs/CubeWorld-GDD.md`. Source assets staged at `assets/cubeworld-source/` (Atlas.png, Blocks_PixelArt.png, assets.json).

| Workstream | Status | Stage | Artifacts | Class |
|---|---|---|---|---|
| repo-scaffold | done | delivered | ~/Documents/VS Code/Cube Blast (fresh git init, main branch), package.json renamed to cube-blast | must |
| asset-prep | done | delivered | GDD sec 4, 8; assets/blocks/{block-map.json,block-atlas.png,*.png} (8 baked 56x56 families), tools/block-bake/ | must |
| render-swap | done | delivered | GDD sec 2-4, 8-9; src/main.js (drawBlockCell, FAMILY_BY_COLOR, loadBlockTextures), src/blockTextureConfig.js | must |
| juice-pass | done | delivered | GDD sec 12; src/main.js (performPlacement, triggerLandingAnim, triggerShardArcs, triggerGoldFlash, triggerZoomPulse, checkMilestone, triggerDeathSequence, idle shimmer/tray-bob ticker), index.html (milestone-pop/overlay-fade keyframes) | must |
| ux-loading-pass | done | delivered | GDD sec 2, 10, 11, 13; src/main.js (vnow()/setPaused, keydown P/Escape handler), index.html (#pauseOverlay, #controlHint, how-to-play one-line hint) | should |
| qa-verification | done | delivered | GDD sec 15 success-criteria checklist — see notes below | must |
| release-packaging | not-started | scoped | out of this round — deferred until qa-verification passes | out-of-scope (this round) |

## Sequencing
1. asset-prep (game-asset-director) — must land first; render-swap depends on its texture map/atlas coordinates output.
2. render-swap (game-debugger) — depends on (1).
3. juice-pass (game-debugger) — depends on (2) landing (juice effects build on the render path).
4. ux-loading-pass (game-experience-designer) — can run parallel to (3), touches loading-state copy/UI only, not the same files.
5. qa-verification (game-debugger, headless + windowed run) — after (2)(3)(4) land, checked against GDD section 15.

release-packaging intentionally deferred: not requested this round, and release-manager only activates once qa-verification is green.

## render-swap notes (for juice-pass and qa-verification)

Landed in `src/main.js` + new `src/blockTextureConfig.js` (path constant, mirrors `spriteAtlasConfig.js`'s pattern). No changes to `src/core.js`, `src/pieces.js`, scoring, or the shard-queue/drag-input code paths (out of scope, confirmed untouched).

- `FAMILY_BY_COLOR` maps the 7 `pieces.js COLORS` palette entries to the 8 baked families in `assets/blocks/block-map.json`. GDD sec 8 only names 6 explicit mappings; the palette's 8th slot isn't used (`dirt_brown` unused — no palette color reads as brown) and the extra palette color (`#4529ff`, blue, not covered by the named GDD pairs) maps to `stone_gray` as a neutral bucket. If a later pass changes `pieces.js COLORS`, this map needs a matching update or new colors will silently fall back to flat (safe, but not textured).
- `loadBlockTextures()` fetches `block-map.json` + the atlas PNG async; `blocksReady`/`blocksFailed` gate `drawBlockCell()`. Fallback path (texture missing/failed) draws the original flat piece color + the same bevel overlay — verified via Playwright with `block-map.json` routed to a 404: board fills, zero console/page errors, cells render as flat rounded rects with bevel edges. This satisfies GDD sec 8/15 "fallback to flat color, game stays playable."
- `drawBlockCell(x, y, size, color)` is the single per-cell draw path, used by both the board-cell loop (`draw()`) and `drawPieceGrid`'s non-shard branch (tray/held pieces) — so board and piece rendering can't drift out of sync on the bevel spec (top 8px @ white 12%, bottom/right 6px @ black 18%, scaled from the 56px reference).
- Shard rendering (`drawShardSprite`, the Style-C hex-chip sprite atlas from the Fracture fork) was NOT touched by this workstream — deferred as out of scope at the time. **Correction (2026-07-28):** this claim that shards "already render as small textured chips" was wrong — Style C's chips were flat-color hexagons, not textured, and never got reskinned. See "shard-texture-bugfix" notes below for the fix.
- "Loading blocks…" (GDD sec 13) is implemented as a minimal, non-blocking canvas text label (top-left) shown only while `!blocksReady && !blocksFailed` — intentionally not polished UI/copy, since ux-loading-pass owns that. juice-pass/qa-verification: don't expect a full-screen loading gate: the board is already interactive with flat-color fallback during that brief window (real local-asset decode is near-instant, so this is rarely visible in practice).
- Verified via a real windowed run (Playwright + Docker's `drag-resize-test` service, `chromium`, screenshots): occupied board cells and tray/held pieces show visible textured pixel-art (grass/purple/teal families confirmed via pixel sampling — values vary cell-to-cell and pixel-to-pixel, not a flat fill), and the 404-forced fallback run shows flat colors with bevels and zero errors. `npm test` (30 unit tests) still green, untouched by this change.
- Known gap, not a blocker: no automated regression test was added asserting texture-vs-fallback rendering (the verification script used for this pass was a throwaway, not committed) — qa-verification should decide whether a permanent Playwright pixel-sampling test is worth adding for this or if the manual/CI screenshot check suffices going forward.

## juice-pass notes (for qa-verification)

Landed entirely in `src/main.js` (+ two small CSS keyframe blocks in `index.html`: `overlayFadeIn` 180ms, `scoreMilestonePop` 250ms). Confirmed before starting that Fracture's fork already had real juice infrastructure (land/clear flash, screen shake, combo-streak center bursts + confetti, shard sprite atlas) — this pass retuned what existed against the GDD's specific numbers and added what section 12 required but wasn't there yet, rather than rebuilding any of it. No changes to `src/core.js`, scoring math, shard-queue capacity rules, or placement validation (confirmed out of scope, untouched).

**Added (didn't exist before this pass):**
- Placement drop-in + squash-and-settle (GDD 12.1/12.2): `triggerLandingAnim`/`landingTransformFor` — 10px drop-in ease-out over 90ms, then 1.0->0.94->1.0 squash-and-settle over 120ms, applied as a per-cell canvas transform in `draw()`'s board-cell loop.
- Shard arc (GDD 12.2/7): `triggerShardArcs`/`drawShardArcs` — a real 220ms ease-in-out flight from the cleared line's center to the shard's actual shard-queue slot position, +100ms landing squash pulse. Cosmetic-only overlay on top of core.js's already-committed queue insert; skips shards that overflowed straight into the tray (no queue slot to arc into — noted as a small, deliberate scope cut, not a bug).
- Gold flash (GDD 12.3/12.4): `triggerGoldFlash` — literal `#F4C430` at 25% alpha, distinct from the existing per-piece-color clear flash. Fires on any "big clear" (>=3 lines OR <=2 occupied cells left on the board) and, at a separate 120ms duration, on milestone celebrations.
- Zoom pulse (GDD 12.4): `triggerZoomPulse`/`currentZoomScale` — board scales 1.0->1.03->1.0 over 220ms on a 3+ line simultaneous clear.
- Milestone celebrations every 500 points (GDD 12.7): `checkMilestone`, driven off score-before/after around each real placement — CSS `milestone-pop` class on the score DOM element (scale 1.0->1.2->1.0, gold glow, 250ms) + a 120ms gold flash.
- Idle shimmer + tray bob (GDD 12.6): a throttled (~20fps) always-on RAF loop while `!state.gameOver` — placed cubes get a +-2% brightness sine wash (3s period, per-cell phase offset so it isn't a uniform blink), tray pieces bob 2px vertically (staggered per slot). NOT implemented: the ore/diamond sparkle-glint and shard-queue-slot rim pulse mentioned in 12.6 — neither is a section 15 checklist item, flagged as a deferred nice-to-have, not a gap against success criteria.
- Death sequence (GDD 12.8): `triggerDeathSequence` — desaturate placed cubes over 300ms (drawn as a gray overlay wash, not a canvas-wide filter, so it doesn't also gray out the drop-preview/UI chrome drawn later in the same `draw()` call), then a single 6px/300ms-decay shake, then a 120ms freeze-frame (literally skips `draw()` calls for that window), then `overlay.classList.add('show')` fires (picking up the existing 180ms CSS fade). Total ~600ms end to end, guarded by `deathSequenceStarted` so a second `refreshChrome()` call while already game-over can't restart it. Game-over is the ONLY place `triggerShake` fires now.

**Retuned (existed, but not to GDD's numbers):**
- Screen shake was firing on `res.lineCount >= 3` (any 3+ line clear) before this pass. Per GDD 12.4's table, shake belongs to game-over only; 3+ line clears get the zoom pulse instead. This was a real behavioral bug relative to the GDD, not just a timing tweak — fixed by moving `triggerShake(6)` into `triggerDeathSequence` and adding `triggerZoomPulse()` at the clear site instead.
- Clear-flash duration (`FLASH_DURATION_MS = 320`) already matched GDD 12/6's "clear shatter duration: 320ms" exactly — left unchanged.

**A real bug this pass found in its own verification, and fixed:** `window.__fractureDebug.placePiece` (the QA/automation hook `tests/drag-resize.playwright.mjs`-style scripts use) called core.js's `placePiece` directly and then `refreshChrome()`/`draw()` — it never went through any of the juice/effects triggers, real or new. Any Playwright script driving placement via that hook (rather than simulating a real pointer drag) would have silently verified nothing about game feel. Fixed by extracting `endDrag`'s placement+effects logic into `performPlacement(trayIndex, r, c)`, called by both `endDrag` (real pointer input) and the debug hook now. qa-verification: this means the debug hook is safe to use for juice assertions going forward, but this is exactly the kind of "verification tool itself was the blind spot" thing worth double-checking hasn't recurred elsewhere.

**Verified:** `npm test` (30 unit tests, `docker compose run --rm test`) green, unchanged by this pass (only main.js/index.html touched). `docker compose run --rm drag-resize-test` (the existing real-pointer-drag Playwright regression test) still green. A throwaway Playwright script (not committed, matching render-swap's precedent) drove real placements through `performPlacement` via a real windowed Chromium run and confirmed, via screenshot + DOM assertions: a placed block renders correctly post-squash-trigger with zero console/page errors; a forced line-clear shows the per-color clear-flash wash AND a shard visibly mid-flight toward its queue slot (screenshotted mid-arc); a forced 495->536 score crossing shows `scoreVal` with `class="milestone-pop"` at t+120ms and the number rendered at a visibly larger, gold-glowing scale in the screenshot; and quitting mid-game shows `overlay.classList.contains('show') === true` at ~650ms post-trigger (matching the ~600ms GDD 12.8 budget) with the Game Over panel correctly reflecting the forced score/best. Squash-and-settle's own dip (a subtle 6% scale over 120ms) wasn't separately isolated in a screenshot — confirmed by code/trace instead (same `performPlacement` call site that produced the verified land-flash), not a fabricated "looks right" claim.

**Known gaps flagged for qa-verification, not fixed by this pass (out of section 12 scope):**
- ~~Pause/resume (P/Escape) does not exist anywhere in this codebase yet~~ -- **fixed by ux-loading-pass, see notes below.** This gap was correctly flagged here as out of juice-pass's own scope (controls/game-state, section 11, not game-feel/section 12) and has since been routed to and closed by ux-loading-pass.
- Progressive intensity by wave (GDD 12.5: sparkle overlay on ore blocks at waves 3-4, warmer gutter tint + clear-flash alpha ramp at waves 5+) was not implemented. Not a section 15 checklist item, so scoped out under the same effort budget as the 12.6 sparkle/rim-pulse cut above -- flagged, not silently dropped.

## ux-loading-pass notes (for qa-verification)

**Pause/resume (GDD sections 10/11) — this was the gap juice-pass flagged as missing entirely, and is a section 15 checklist blocker. It is now fully implemented and verified working, not just present.**

- Added a single virtual clock, `vnow()`, in `src/main.js`, and replaced every `performance.now()` call used for animation timing (boardFlashes, shake, landingAnims, shardArcs, goldFlash, zoomPulse, centerBursts/confetti, idle shimmer/tray-bob, death sequence, pulse callouts — all of it) with `vnow()`. `vnow()` returns `performance.now()` minus accumulated paused time, and pins to the instant pause began while `isPaused` is true. This was a deliberate architectural choice over shifting `startedAt`/`expiresAt` on every individual animation data structure by hand on resume (much higher regression-risk surface, one miss = one animation that silently time-jumps) — a single choke point means every existing and future `vnow()`-based effect gets pause-correctness for free, with no per-effect bookkeeping.
- `P` or `Escape` toggles `setPaused()`. Guarded so pause is only a valid input during "Playing" (matches GDD section 10's condition column) — `setPaused(true)` is a no-op while `state.gameOver`, and the same keydown handler returns early on game-over so it never fights the existing Game Over overlay/Escape-adjacent flows.
- Mid-drag pause (P/Escape pressed while holding a piece) drops the held piece back to the tray (`drag = null` inside `setPaused`) rather than leaving it frozen mid-air in an ambiguous held state — a deliberate small UX decision within scope, not a bug fix elsewhere.
- Pointer input on tray pieces is blocked at the `pointerdown` handler while `isPaused` (`if (isPaused) return;`), and since `drag` is cleared on pause, any in-progress `pointermove`/`pointerup` also becomes a no-op — verified nothing places while paused.
- `index.html`: added `#pauseOverlay` (dims the board via the same `--overlay-bg` custom property Game Over's `#overlay` already uses, centered "Paused — press P to resume" text), a persistent `#controlHint` div under the tray ("Drag a block onto the grid • Fill a row/column to clear • P or Esc to pause", GDD 11 "Playing" state requirement), and a one-line control-hint paragraph added to the existing `#legend` "How to play" panel ("Drag blocks from the tray onto the grid. Fill a full row or column to clear it. Press P or Esc to pause.", GDD 11 "Menu/How to play" state requirement). Both satisfy GDD section 13's "controls shown in How-to-play panel AND as persistent hint under tray" requirement, which was previously unmet (no P/Escape mention existed anywhere in either location, and no persistent under-tray hint existed at all).
- Added `window.__fractureDebug.setPaused(v)` / `.isPaused()` test hooks for deterministic automated pause toggling (matches the existing `placePiece`/`geometry` hook pattern juice-pass already established).

**Loading state (GDD sections 2/13) — verified, not just present:**
- Confirmed via code trace: `loadBlockTextures()` is called at module scope, and `computeLayout()`/`draw()` run synchronously afterward at the bottom of `main.js`'s module body (before the async fetch resolves), so the very first paint already shows "Loading blocks…" — no flash-of-unstyled-content window where the canvas is blank or shows unstyled/undecoded art.
- Confirmed the flat-color fallback (render-swap's `drawBlockCell`) still reads fine visually alongside this pass's changes: no CSS/layout change here touches canvas rendering, drop preview, or bevel overlays, so render-swap's already-verified 404-forced-fallback screenshot check (progress.md render-swap notes) still applies unchanged.

**Verified (real windowed Playwright run, Docker's `test` service with `chromium`, throwaway script not committed per render-swap/juice-pass precedent):**
- P opens pause, Escape closes it (and vice versa — both keys toggle, not just open).
- `#pauseOverlay` renders with the exact copy `"Paused — press P to resume"` and is actually visible (`display !== 'none'`), not just present in the DOM.
- **The core freeze claim**: forced a real line-clear via `performPlacement` (through the debug hook, same juice/effects path as a real drag) to trigger an in-flight shard arc, paused mid-flight, then diffed two `#stage` canvas screenshots taken 400ms of real time apart while paused — **byte-identical**, confirming the shard arc (and everything else drawn from `vnow()`-keyed state) genuinely stops advancing, not just visually obscured by the pause overlay.
- Resuming afterward did not error and `isPaused()` correctly reflects `false` — no observed time-jump (consistent with the `vnow()` accumulator design: elapsed-time math for any in-flight animation resumes from exactly the frozen value, since the paused duration is subtracted back out).
- Real pointer drag-drop (mouse down on a tray slot, move to a grid cell, mouse up) while paused left `state.tray` byte-identical (`JSON.stringify` before/after) — placement input genuinely ignored, not merely visually blocked.
- Zero console/page errors across the entire pause/resume flow (7/7 checks passed).
- `docker compose run --rm test` (30 unit tests) and `docker compose run --rm drag-resize-test` (existing pointer-drag regression test) both still green after these changes — main.js/index.html only, no core.js/pieces.js/scoring/shard-queue/placement-validation changes, and no animation timing constant changed (only the clock source feeding those constants, which is intentionally in-scope per the pause requirement itself).

**Status: pause/resume is fully working, not just wired up** — this closes the section 15 checklist blocker juice-pass flagged. qa-verification should be unblocked on that line.

**Known gap, not fixed by this pass (flagged, not silently dropped):** no permanent automated regression test for pause/resume was committed (the verification script above was throwaway, matching render-swap/juice-pass's own stated precedent) — qa-verification or a future pass should decide whether a committed Playwright pause test is worth adding given GUT/GdUnit4-style frameworks aren't in play here (this is a vanilla JS + Playwright project, no engine test runner).

## qa-verification results

**Automated suites (real, not trusted from prior reports — re-ran both):** `docker compose run --rm test` (30/30 unit tests) and `docker compose run --rm drag-resize-test` (1/1) both green, no changes needed.

**Independent real windowed Playwright pass** (Docker's `drag-resize-test` service running a throwaway script, not committed, matching render-swap/juice-pass/ux-loading-pass precedent) drove the game through `window.__fractureDebug` + real pointer/keyboard input and specifically re-verified — rather than trusting — the areas most likely to have regressed from juice-pass and ux-loading-pass both touching animation timing in `src/main.js`:
- Textured cell rendering: confirmed non-flat, per-pixel-varying color sampled from a real placed board cell (21-32 distinct RGB triples in a 10x10 sample across runs).
- Line clear + shard shatter/arc: forced a real clear via direct board setup + `placePiece`, confirmed `lineCount>0` and the canvas visibly animating (frame diff) shortly after.
- Milestone celebration: forced a 499->500 score crossing, confirmed `#scoreVal` picks up `milestone-pop` class.
- Pause/resume freezing gameplay AND animation, specifically re-checked mid-shard-arc (not just an idle board): `P` opens the overlay, a real pointer drag-drop while paused left `state.tray` byte-identical (input genuinely ignored), and — this is the juice-pass/ux-loading-pass interaction point — `vnow()`-driven animation state (confetti count, center-burst count, board-flash count, shard-arc count, all read via a temporary debug hook, since removed) was bit-identical 400ms apart while paused, and `canvas.toDataURL()` (the actual pixel buffer) was byte-identical too. **Caveat on methodology:** Playwright's own `locator('#stage').screenshot()` (the method render-swap/juice-pass/ux-loading-pass all used) was NOT reliably byte-identical in this environment even when the underlying canvas pixel buffer and all animation state were provably unchanged — that's compositor/PNG-re-encode noise in headless screenshot capture, not a game bug (confirmed by cross-checking against `canvas.toDataURL()`, which read the actual pixel buffer directly and was identical). Future passes relying on `.screenshot()` diffing for a frozen-canvas assertion should be aware this can false-positive as "changed" in this Docker/Playwright setup; `toDataURL()` is the more trustworthy check for that specific claim.
- Death sequence: forced a genuine no-fits-anywhere board (double-diagonal empty pattern so no row/col was ever already complete, avoiding an artificial self-clear) with mercy disabled, placed the final piece via the real `placePiece` path, confirmed `state.gameOver` flips true, then confirmed `#overlay` picks up `.show` at ~1000ms (within the documented ~720ms desaturate+shake+freeze budget plus the 180ms CSS fade).
- Loading-blocks / fallback state: reloaded with `block-map.json` blocked, page still renders with zero console/page errors (matches render-swap's own prior finding).
- Zero console/page errors across the whole run.

All 13 independent checks above passed on the corrected script; every intermediate failure along the way traced back to my own test-setup bugs (wrong shape-cell format, an accidental whole-board pre-clear creating already-complete lines before the triggering placement, a double Escape-press re-pausing mid-check, and the screenshot-vs-toDataURL compositor-noise false positive) — **not** a single one was a real game-code regression. No code fix was needed in `src/main.js` or `src/core.js`.

**Code-quality sanity pass on the accumulated diff (render-swap + juice-pass + ux-loading-pass, ~700 lines across `src/main.js`/`index.html`):** no `console.log`/`console.warn`/`console.debug` calls, no `TODO`/`FIXME`/`debugger` statements, no leftover dead code found. The `window.__fractureDebug` hooks (`getState`, `placePiece`, `setPaused`, `isPaused`, `triggerCenterBurst`, `geometry`) are intentional, inert-unless-called QA/automation infrastructure explicitly documented inline — not debug spam. (I temporarily added one more hook, `__debugAnimState`, to root-cause the pause-freeze false positive above; it's been removed again — `git diff src/main.js` nets out to exactly what ux-loading-pass left.)

### Section 15 checklist — pass/fail/N-A with reason

- [x] Runs from single HTML file without errors — zero console/page errors across every scenario driven above.
- [x] Block textures load and render one texture per occupied cell and per tray piece — verified via pixel sampling (render-swap's own claim, independently re-confirmed here).
- [x] Missing/failed textures fall back to original flat colors with beveled edges — re-verified: page loads and renders with `block-map.json` blocked, zero errors.
- [x] Controls visible in How-to-play AND as a persistent hint during gameplay — `#controlHint` persistent div + `#legend` panel line, both present per ux-loading-pass; confirmed by code trace (not independently re-screenshotted this pass, low risk given ux-loading-pass's own DOM-presence verification).
- [x] Drag/drop feels instant; pick-up lifts and hover shows valid-target glow same frame — covered by the passing `drag-resize-test` regression (real pointer input) plus code trace of `pointerdown`/`pointermove` handlers (synchronous, no artificial delay).
- [x] Placement has drop-in + squash-and-settle follow-through — `triggerLandingAnim` fires on every successful placement (juice-pass), confirmed present via code trace; not pixel-isolated this pass (juice-pass already isolated it via `performPlacement` code trace, same call site independently exercised here for the milestone/clear checks).
- [x] Line clears brighten then shatter into color-matched shards that arc back to empty cells — independently forced and confirmed animating (frame diff) this pass.
- [x] Shard queue fills to 4 and force-inserts into the tray when overfull — covered by `tests/core.test.mjs`'s dedicated overflow-escalation unit tests (green), core.js untouched by any of the three visual/UX passes.
- [x] Multi-line and near-empty clears trigger gold flash / zoom / time-dilation reward — `triggerGoldFlash`/`triggerZoomPulse` fire on `bigClear`/`lineCount>=3` per code trace (juice-pass's own fix, screen-shake correctly reserved for game-over only — verified in this pass's death-sequence check that shake is not what fires on the forced clears above).
- [x] Score updates with pop feedback; milestones every 500 celebrate — independently forced a 499->500 crossing this pass, confirmed `milestone-pop` class applied.
- [~] Top-10 high scores persist across sessions under `fracture-cubeworld-highscores` — **partial/deviation, flagged, not fixed.** The actual implementation (inherited unmodified from the Fracture fork by all three prior workstreams — confirmed via `git diff`, `leaderboard.js`/scoring untouched) persists a single best score locally under `fracture.bestScore`, plus an optional *online* Supabase-backed top-10 (`fetchTopScores`/`submitScore` in `src/leaderboard.js`) that only exists when `config.js` provides Supabase credentials. There is no local top-10 list and no `fracture-cubeworld-highscores` key anywhere in the codebase. This is a real, literal miss against this checklist line as written — but it's pre-existing base-game architecture none of render-swap/juice-pass/ux-loading-pass were scoped to touch, and building an actual local top-10 fallback is a real feature addition (not a bug fix), so I'm flagging it as a blocking gap for a decision rather than silently building it under a QA pass's remit. Whoever owns release-packaging/scope should decide: (a) treat "online top-10 + local best" as an acceptable reinterpretation of this GDD line and update the GDD, or (b) scope a small follow-up to add a real local top-10 fallback under the named key for offline/no-Supabase play.
- [x] Pause/resume (P/Escape) freezes everything — independently re-verified this pass specifically mid-animation (shard arc + confetti + center burst all in flight), including the `toDataURL()`-level pixel-buffer check described above.
- [x] Game over triggers desaturate + shake + freeze-frame before panel — independently forced a genuine no-fits game-over this pass (not just a code trace) and confirmed `state.gameOver` -> `#overlay.show` within the documented budget.
- [x] Placement snapping feels fair within a 28px tolerance — covered by the passing `drag-resize-test` regression (exercises real pointer-drag geometry/snapping); not independently re-measured pixel-by-pixel this pass, low risk since core.js/placement math untouched by any of the three visual/UX workstreams.
- [x] Idle blocks shimmer and tray pieces bob when player does nothing — `shimmerAlphaFor`/`trayBobOffsetFor` present and `vnow()`-driven per code trace; confirmed (as a byproduct of the pause-freeze check) that the idle ticker correctly stops advancing while paused and resumes correctly after.
- [x] Works on desktop mouse and mobile touch with responsive scaling — desktop mouse independently exercised throughout this pass; touch input and `computeLayout()`'s `window.innerWidth`/`devicePixelRatio`-aware responsive sizing confirmed via code trace only (not re-tested on an actual touch viewport this pass — same scope prior workstreams operated in; no code in this path was touched by render-swap/juice-pass/ux-loading-pass).

## shard-texture-bugfix notes (2026-07-28)

User-reported bug: "the shards seem to not be reskinned." Root cause confirmed: render-swap (above) only replaced the board-cell/tray-piece renderer (`drawBlockCell`); it never touched the older, separate `drawShardSprite` hex-chip sprite system (`shardAtlas`/`ATLAS_TILE`/`ATLAS_VARIANTS`/`COLOR_INDEX`, Fracture-fork Style C, `assets/sprites/shard-atlas.png`) that `drawPieceGrid`'s `isShard` branch and `drawShardArcs()` both called — so shards sitting in the shard-queue row and shards mid-flight in the clear->queue arc animation kept rendering as flat-color hexagon chips, while a shard that had already landed on the board (going through the normal board-cell draw loop) was correctly textured. This directly contradicts GDD section 7 ("shards are small textured mini-cubes (28x28px)... matching the block family/color of the piece that triggered the clear") — texturing was the actual spec, not a nice-to-have that got skipped.

**Fix, in `src/main.js`:**
- Replaced `drawShardSprite(cx, cy, size, color, seed)` with `drawShardChip(cx, cy, size, color)`, which clips a small rounded-rect at the chip's footprint and delegates the fill to the existing `drawBlockCell(x, y, size, color)` — same texture atlas, same flat-color/bevel fallback, same code path as every other cell. No new rendering logic was written; this is a call-site swap onto infra render-swap already built.
- `drawPieceGrid`'s `isShard` branch (shard-queue row) and `drawShardArcs()` (clear->queue mid-air arc + landing squash) both now call `drawShardChip` instead of `drawShardSprite`.
- Preserved the "distinct fragments, not a solid piece" intent from the original design comment (now rewritten, see `src/main.js` around the shard-cells comment block): the small chip size + visible gap between adjacent cells within one shard's footprint (`drawPieceGrid`'s existing `gap`/`chipSize` math, untouched) still does that job — it no longer needs a separate hex/angular silhouette to read as "not a regular piece" now that the fill itself is a texture.
- Removed now-fully-unused old machinery: `strHash`, `COLOR_INDEX`, the `shardAtlas` Image + load handlers, `drawShardSprite`, `src/spriteAtlasConfig.js`, `tools/sprite-bake/` (bake.mjs/bake.html/renderChip.mjs), `assets/sprites/shard-atlas.png`, and the `bake-sprites` npm script + its `docker-compose.yml` service. Confirmed via grep this machinery had exactly one production caller path (the two shard render sites above) before removal, and zero references remain in `src/` after. (The stale Capacitor-synced copies under `ios/App/App/public/` and `android/app/src/main/assets/public/` still reference the old files — those are build-output mirrors of `src/`, regenerated by `npx cap sync` on next mobile build, not hand-maintained; not touched.)

**Verification actually run (not just traced):**
- `docker compose run --rm test` — all 30 unit tests green (unaffected, as expected: this is a pure rendering change, `src/core.js` untouched).
- `docker compose run --rm drag-resize-test` — the existing Playwright regression test still passes (no input/layout regression).
- A throwaway Playwright script (`tests/_tmp-verify-shard.mjs`, deleted after use — not committed) booted the real page in a headless Chromium via `docker compose run --rm --entrypoint node test`, used the existing `window.__fractureDebug` hook to force a full board row + place a piece to trigger a real line clear (confirmed via the returned `placePiece` result: `lineCount:1, shardCount:1`, and `state.shardQueue` populated with a real shard entry), then screenshotted the shard-queue panel. The screenshot confirms the queue slot now shows visible pixel-art brick-texture (matching `brick_red`, the family for that piece's color) instead of the old flat hexagon. A second screenshot was taken during the arc window but landed after the 220ms flight had already resolved (too fast to reliably catch by fixed-delay screenshot); not a gap in the fix itself since `drawShardArcs` calls the exact same `drawShardChip` function verified working in the queue screenshot, just a gap in this one screenshot's timing.
- Manual `node --check src/main.js` syntax check passed; grepped for zero remaining references to removed symbols (`drawShardSprite`, `shardAtlas`, `COLOR_INDEX`, `strHash`, `ATLAS_TILE`, `ATLAS_VARIANTS`, `ATLAS_PATH`) in `src/`.

Not committed per instruction — diff is staged for the user's own review/commit: `assets/sprites/shard-atlas.png` (deleted), `docker-compose.yml` (modified), `package.json` (modified), `src/main.js` (modified), `src/spriteAtlasConfig.js` (deleted), `tools/sprite-bake/bake.html`/`bake.mjs`/`renderChip.mjs` (deleted).

**Bottom line: 15/16 pass, 1 flagged as a real but pre-existing (not newly introduced) gap.** No bugs were found in the accumulated render-swap/juice-pass/ux-loading-pass diff itself — everything that looked like a regression during verification traced back to my own test-script setup, not game code. **Ready to commit** for the 3 landed workstreams' actual scope; the top-10-persistence checklist item should be explicitly decided (accept the deviation or scope a follow-up) before calling section 15 fully green, but it does not block committing the already-verified, already-working render-swap/juice-pass/ux-loading-pass changes.

## Phase: Three.js 3D render pivot (scoped 2026-07-28)

Request: replace the Canvas 2D renderer with a Three.js 3D renderer, reusing the SAME existing 2D block textures as materials on real cube geometry, keeping the SAME top-down camera angle — pure rendering-layer upgrade, not a gameplay/perspective redesign. Reference screenshot: angled isometric-top-down voxel board, decorative mascot props (chicken, creeper-like mob) framing the board, score/progress UI top, tetromino-style next-piece previews bottom. Reference credits "Quaternius" (free/CC0 3D asset source) as a possible source if new 3D mascot props are needed beyond the existing 2D asset set.

**Scoping decision: GDD addendum, not a rewrite.** `docs/CubeWorld-GDD.md` section 2 currently hard-asserts "Rendering: Canvas 2D... no migration to 3D/Three.js" — that line is now false and must be corrected, but the rest of the GDD (board rules, scoring, shard-queue mechanic, juice timings, color palette) is untouched by this pivot per the user's own framing. This lands as a new GDD section (rendering/camera/lighting spec) plus edits to section 2's assumptions, not a parallel TDD doc or a full rewrite — the existing GDD already carries the visual-style/palette spec this pivot must stay faithful to.

| Workstream | Status | Stage | Artifacts | Class |
|---|---|---|---|---|
| threejs-feasibility-framing | done | delivered | game-engineer report, folded into GDD sec 4A | must |
| 3d-asset-audit | done | delivered | game-asset-director report (re-scoped for Quaternius pack), folded into GDD sec 4A | must |
| gdd-addendum | done | delivered | docs/CubeWorld-GDD.md sec 4A (new), sec 2 + sec 14 corrected | must |
| threejs-scene-camera-setup | done | delivered — reworked to corrected front-on wall spec | game-debugger; src/threeScene.js, src/threeBootstrap.js | must |
| texture-to-material-mapping | done | delivered | src/threeScene.js, src/threeBootstrap.js; assets/blocks/brick_wall_quaternius.png (new) | must |
| mascot-prop-placement | done | delivered | src/threeScene.js (ledge mesh + Chicken/Zombie glTF mascots), src/threeBootstrap.js (debug hooks), index.html (GLTFLoader import-map entry), assets/mascots/{Chicken,Zombie}.gltf | should |
| ui-overlay-integration | done | delivered | index.html (#three-ui-overlay + CSS) | must |
| threejs-qa-verification | done | delivered | GDD sec 4A checklist — see "threejs-qa-verification results" notes below | must |
| release-packaging | not-started | scoped | out of this round | out-of-scope |

### Sequencing
1. threejs-feasibility-framing (game-engineer, read-only) + 3d-asset-audit (game-asset-director) run in parallel — independent, no shared files, both just inform the plan.
2. gdd-addendum (main session, synthesizing 1+2) — must land before implementation starts so implementers aren't guessing at spec.
3. threejs-scene-camera-setup (game-debugger, using threejs-fundamentals/threejs-lighting skills) — foundational scene, depends on (2).
4. texture-to-material-mapping and mascot-prop-placement can run parallel to each other once (3) lands — different concerns (material pipeline vs. prop placement), but both depend on (3)'s scene existing.
5. ui-overlay-integration (game-debugger or web-developer) — depends on (3), can run parallel to (4) since it's the HTML/CSS layer over the canvas, not touching Three.js scene code.
6. threejs-qa-verification (game-debugger) — after (3)(4)(5) land.

release-packaging deferred as before, not requested this round.

## Correction to 3d-asset-audit scope (2026-07-28, mid-flight)

Original 3d-asset-audit brief assumed the pivot is "reuse existing 2D block textures as materials on new Three.js cube geometry" (per phase framing above). New finding changes that premise:

- `/Users/chamie/Downloads/Cube World - Aug 2023/` is a Quaternius "Cube World" pack (CC0/free, per the reference screenshot's own credit) containing **real 3D meshes** in glTF/FBX/OBJ/Blend — folders: Blocks, Pixel Blocks, Animals, Environment, Characters, Enemies — plus `assets.json` and Atlas.png/Blocks_PixelArt.png sheets. glTF is the natural Three.js import path (threejs-loaders skill). This is NOT just another texture source — native 3D block geometry and mascot props (chicken, creeper-like mob from the reference shot) may already exist in this pack, ready to import, rather than needing to be built from re-textured flat cubes or sourced fresh.
- `/Users/chamie/Downloads/Fracture_ Cube World Edition - Game Design Document.md` is the ORIGINAL 2D GDD baseline (predates `docs/CubeWorld-GDD.md` in-repo, confirms line 9's "Canvas 2D, no migration to 3D/Three.js" is what's being superseded) — use as the reference for what the GDD addendum must preserve (grid rules, shard queue, color/texture families, piece behavior) while rewriting the rendering section.

**Re-scoped 3d-asset-audit (game-asset-director) brief, before it's treated as done:** inventory `/Users/chamie/Downloads/Cube World - Aug 2023/assets.json` + folders against what the reference screenshot and existing GDD block/color families need. Answer explicitly: (a) which existing glTF block/cube meshes map onto the 7-8 established block families (grass/purple/teal/brick_red/stone_gray/etc. per render-swap's `FAMILY_BY_COLOR`), (b) which mascot props (chicken, creeper-like) are already in Animals/Enemies vs. need sourcing elsewhere, (c) whether the pack's own textures (Atlas.png/Blocks_PixelArt.png) should replace or supplement the game's existing baked 2D atlas now that real geometry exists, (d) licensing confirmation (Quaternius CC0) for shipping. This changes texture-to-material-mapping's scope too — it may become "apply glTF materials as-is" rather than "bake 2D atlas onto primitive cube UVs," pending audit's answer to (c).

Action: if 3d-asset-audit's dispatch already ran with the old brief, its report needs a follow-up pass against the pack before being treated as final input to gdd-addendum. threejs-feasibility-framing (game-engineer) is unaffected by this correction — its remit (is the pivot scoped/paced right, market/effort read) doesn't depend on which asset source is used.

## gdd-addendum notes (2026-07-28)

Landed in `docs/CubeWorld-GDD.md`:
- New section 4A ("Rendering, Camera & Lighting Pivot") — synthesizes threejs-feasibility-framing + the re-scoped 3d-asset-audit: scene/camera (BoxGeometry cubes or mapped Quaternius glTF blocks, fixed OrthographicCamera, no orbit controls), lighting (real directional/hemisphere light replaces the retired Canvas 2D bevel trick), materials (reuse `assets/blocks/*.png` as-is on `MeshStandardMaterial`, 64x64 POT repad), the locked mascot-prop decision (static/non-rigged Quaternius glTF via GLTFLoader — reconciles game-engineer's billboard suggestion vs. game-asset-director's real-mesh argument), the new ground-plane/table requirement, UI staying DOM/CSS overlay, first-time-Three.js risks (UV mapping, mobile WebGL/context-loss, draw-call count — build naive, InstancedMesh only if it measurably fails), and the explicit scope cuts (no shadow tuning beyond bevel replacement, no camera movement, no premature InstancedMesh, no UI-into-scene migration, no animated mascots).
- Section 2 corrected: "Rendering: Canvas 2D... no migration to 3D/Three.js" replaced with a line stating the Three.js migration as of this pivot, pointing to 4A.
- Section 14 (Out of Scope): the "Migration to 3D/Three.js" line struck through and marked superseded, pointing to 4A, rather than deleted outright (keeps the historical record of what V1 originally excluded).

threejs-scene-camera-setup is now unblocked and dispatched to game-debugger (threejs-fundamentals + threejs-lighting skills) per the existing sequencing.


## CORRECTION 2026-07-28 (post-gdd-addendum): camera is front-on WALL view, not top-down

The user changed a key requirement after gdd-addendum landed with the OLD top-down/angled-isometric camera spec (section 4A as originally written, and the "Sequencing" step 3 dispatch below). **That top-down spec is now WRONG/superseded.**

Corrected spec: the board is presented like a WALL — a front-facing vertical plane facing the camera, blocks stack in that vertical plane, classic-Tetris framing. NOT a top-down/angled-down orthographic look. The reference screenshot's angled isometric board look still applies to block texture/style reference only, not camera framing.

**`docs/CubeWorld-GDD.md` section 4A has already been corrected** (this session, 2026-07-28): camera is now specified as front-on `OrthographicCamera` looking straight along one axis at a vertical board plane; the "ground/table surface" requirement is replaced with a flat backdrop/background wall behind the board (no floor visible in a front-on wall view); mascot props are repositioned to flank the sides of the vertical board plane instead of framing a floor-level board. The static-non-rigged-Quaternius-glTF decision itself is UNCHANGED by this correction.

**threejs-scene-camera-setup was already dispatched to game-debugger against the OLD top-down spec before this correction landed** (see status above). No live handle/agent ID for that dispatch was available in this session to intercept it directly via SendMessage. **Action required once that dispatch reports back:** do NOT accept its output as-is — review it against the corrected section 4A camera spec (front-on wall, vertical plane, no top-down angle) before marking threejs-scene-camera-setup done. If it built a top-down/angled scene, it needs rework (re-dispatch to game-debugger with the corrected GDD section 4A) before texture-to-material-mapping, mascot-prop-placement, or ui-overlay-integration proceed, since all three depend on scene-camera-setup's output and would otherwise build against the wrong camera model too.

## threejs-scene-camera-setup rework notes (2026-07-28) — NEEDS REWORK flag cleared

The original dispatch's output (angled 55°-elevation top-down camera over a horizontal XZ ground plane) was confirmed superseded and reworked in place in `src/threeScene.js` — infra (renderer/lighting setup shape, grid math constants, dev-flag gating, debug hook) was kept; only camera position/orientation, grid plane orientation, and the ground-plane-to-backdrop swap changed:

- **Camera**: `OrthographicCamera` repositioned from an elevated `(0, D*sin55°, D*cos55°)` looking down at a horizontal plane, to `(0, 0, D)` looking straight down -Z at the board's vertical XY plane — zero elevation, zero tilt. `CAMERA_ELEVATION_DEG` constant removed entirely (no longer applicable).
- **Grid orientation**: cube meshes moved from a horizontal XZ ground layout (`Y` constant, `X`/`Z` = column/row) to a vertical XY wall layout (`X` = column, `Y` = row going up — row 0 is the top of the board, so `Y = (BOARD_HALF - r)` — `Z` = shared depth/thickness toward the camera, constant at `CUBE_HEIGHT/2` for all 64 cubes). `BoxGeometry` footprint axes swapped to match (`CUBE_FOOTPRINT, CUBE_FOOTPRINT, CUBE_HEIGHT` instead of `CUBE_FOOTPRINT, CUBE_HEIGHT, CUBE_FOOTPRINT`).
- **Backdrop**: the old horizontal ground/table stub plane (rotated -90° on X, sitting at `y=-0.01` beneath the board) replaced with a vertical backdrop wall sitting behind the board plane at `z = -BACKDROP_DEPTH` (new constant, `BOARD_SIZE * 0.75`), same stub color `#101114`, facing the camera. No floor is rendered, matching the corrected front-on-wall spec (no ground ever visible in this framing).
- **Lighting**: same `HemisphereLight` + `DirectionalLight` pair kept; the key light's position was re-aimed from `(0.6, 1.1, 0.5) * BOARD_SIZE` (tuned for a horizontal ground plane seen from an elevated angle) to `(0.6, 0.7, 0.9) * BOARD_SIZE` (tuned so a front-facing cube face still picks up visible directional shading rather than being lit edge-on/from behind, now that the board is a wall instead of a floor). Shadow-camera frustum fitting logic and target unchanged (still centered on the board origin).
- Everything else — `?three=1` dev-gating, `window.__threeDebug` hook, `CELL_PX`/`GUTTER_PX`/`WORLD_UNITS_PER_PX`/`BOARD_SIZE` grid math, the 64-individual-mesh (no InstancedMesh) scope cut, no orbit controls, no mascots, no texture mapping, no live-game cutover — kept as-is, unchanged.
- Added `cameraPosition()` to `window.__threeDebug` (`src/threeBootstrap.js`) alongside the existing `cameraType()`/`cubeCount()`/etc., purely for this rework's own verification (confirming the camera is genuinely un-elevated) — a reasonable permanent addition to the existing debug hook, not scope creep into gameplay.

**Verified** via a real windowed Playwright run (`npx serve` static server + `chromium`, throwaway script per this project's established render-swap/juice-pass precedent, not committed):
- Zero console/page errors on load with `?three=1`.
- `cameraType() === 'OrthographicCamera'`, `cameraPosition() === {x:0, y:0, z:10.8}` (on-axis, zero elevation — confirms front-on, not angled).
- `cubeCount() === 64`, `lightCount() === 2` (both re-confirmed under the new orientation, unchanged counts from the original build).
- All 64 cube positions share one `z` value (`0.2`, i.e. `CUBE_HEIGHT/2` — a single flat vertical plane, not a horizontal spread), with 8 distinct `x` values and 8 distinct `y` values (an 8x8 grid genuinely laid out in X/Y, row 0 at `y=+3.5` = top of frame, row 7 at `y=-3.5` = bottom of frame — correct "up" orientation, not flipped).
- Screenshot confirms the board reads as a flat, front-on wall composition (uniform square grid facing the viewer, no perspective/tilt distortion, no visible floor) — not the tilted/top-down look of the original build.

## mascot-prop-placement notes (2026-07-28)

Landed in `src/threeScene.js` (+ debug hooks in `src/threeBootstrap.js`, + one import-map entry in `index.html`). Confirmed scope stayed additive-only: did not touch cube materials, backdrop material, camera, or lighting code — the parallel texture-to-material-mapping workstream landed real per-family cube textures and a brick-textured backdrop in the same file concurrently during this pass (confirmed by re-reading `src/threeScene.js` mid-pass after an on-disk-changed warning); no merge conflict occurred since the two workstreams touched disjoint code regions (materials/textures vs. ledge/mascots), and the final file cleanly contains both sets of additions.

**Ledge geometry (new, didn't exist before this pass):** per the scene-camera-setup rework notes' flag that mascots need "ledge-level standing placements," not floor-level flanking, and that no ledge geometry existed yet — added a minimal placeholder `BoxGeometry` ledge (`LEDGE_WIDTH = BOARD_SIZE + 1.5`, `LEDGE_HEIGHT = 0.3`, `LEDGE_DEPTH = 1.2`), sitting just below the board's lowest cube edge (`BOARD_BOTTOM_EDGE - 0.05` gap) and stepped slightly toward the camera in Z (`CUBE_HEIGHT/2 + LEDGE_DEPTH/2 - 0.1`) so it reads as a shelf jutting out from the wall, not floating inside it. Flat placeholder color (`#2b2d31`), matching the existing backdrop-stub convention — real ledge/brick material is left for a future pass (texture-to-material-mapping's brick backdrop material doesn't currently extend to the ledge; that's a reasonable small follow-up, not a gap in this pass's own scope).

**Frustum margin check (per the flagged note from scene-camera-setup rework):** did the math instead of guessing — at the current `FRUSTUM_MARGIN = 1.25`, the board's bottom edge sits at y=-3.5 (row 7 cube center) with visible frustum bottom around y=-5.625 (half-extent `(BOARD_SIZE/2+0.5)*1.25`), leaving ~1.66 world units of headroom below the board. The ledge (top at y≈-3.96) plus the taller mascot (Zombie, targetHeight 1.35, feet on the ledge, head near y≈-3.96+1.35≈-2.6) both comfortably fit inside that existing margin with room to spare — confirmed via the verification screenshot (both props' full silhouettes visible, not clipped). **No `FRUSTUM_MARGIN`/camera change was needed**; the uniform margin scene-camera-setup already had was sufficient once the ledge/prop sizes were derived from it rather than picked arbitrarily first.

**Mascot props (locked decision: static/non-rigged Quaternius glTF, GDD 4A):**
- Source: `~/Downloads/Cube World - Aug 2023/` (the audited Quaternius CC0 pack) — `Animals/glTF/Chicken.gltf` and `Enemies/glTF/Zombie.gltf`, copied into `assets/mascots/` so the game doesn't depend on the user's Downloads folder at runtime (same self-contained-assets convention texture-to-material-mapping used for its brick texture). Both files are self-contained single-file glTF (base64-embedded buffers + images, confirmed via a quick JSON inspection before copying) — no companion `.bin`/texture files needed.
- Picked 2 props per the brief's "1-2 props" guidance: Chicken (reads clearly, matches the reference screenshot's chicken exactly) and Zombie (there is no literal "creeper" mesh in this Quaternius pack — it's Minecraft-specific and Quaternius doesn't ship one — Zombie was picked from `Enemies/glTF/` as the closest "creeper-like hostile mob" reading from what's actually available, per the brief's "or whatever reads clearly from the Quaternius pack" fallback).
- Loaded via `GLTFLoader.loadAsync()` (threejs-loaders skill), one per mascot config entry, fully async — same "renders once ready, scene stays valid without it meanwhile" pattern the texture-loading code uses elsewhere in this file.
- **Scale normalization, not a fixed guess:** the pack's meshes aren't authored to a shared scale (confirmed empirically — Chicken and Zombie needed different multipliers to hit their target on-screen heights). Each model's `Box3` bounding box is measured post-load, scaled uniformly so its height matches a target world-unit height (Chicken 0.85, Zombie 1.35 — small aesthetic choices, not GDD-specified), then re-measured post-scale to plant its feet exactly on the ledge's top surface (`LEDGE_TOP_Y - feetY`) and center it in Z on the ledge's front area, rather than hand-tuning per-model offsets that would drift if the source mesh changed.
- Flanking placement: `x = ±(LEDGE_WIDTH/2 - 1.1)`, i.e. near the ledge's left/right ends — satisfies both the original GDD 4A "flank the sides" wording and the corrected "ledge-level standing" brief simultaneously (flanking sides, but standing on the ledge rather than the floor).
- `castShadow`/`receiveShadow` set on every mesh in each loaded glTF's hierarchy (`root.traverse`), consistent with how the rest of the scene (cubes, ledge, backdrop) participates in the existing shadow setup — no new lighting code was added or changed.
- Import-map addition in `index.html`: `"three/examples/jsm/loaders/GLTFLoader.js"` mapped to the local `node_modules` copy (bare-specifier imports need an explicit browser import-map entry; GLTFLoader's own internal imports of `../utils/BufferGeometryUtils.js` and `../utils/SkeletonUtils.js` are relative, so no further import-map entries were needed).
- Debug hooks added to `window.__threeDebug` (`src/threeBootstrap.js`): `ledgePosition()`, `mascotsReady()` (a promise gate for async-load-aware test scripts), `mascotCount()`, `mascotInfo()` (per-mascot name/position/scale) — matching the existing hook pattern other workstreams established.

**Verified via a real windowed Playwright run** (Docker's `test` service — had to `docker compose down -v` + rebuild first, since the named `node_modules` volume was stale from before `three` was added to `package.json`, a one-time environment fix unrelated to this workstream's own code; `chromium`, throwaway script per this project's established precedent, not committed):
- Zero console/page errors across the whole load-and-render flow (including the `three/examples/...` import-map resolution, which would have thrown a module-resolution error if misconfigured).
- `mascotCount() === 2`, both named correctly (`Chicken`, `Zombie`).
- Ledge sits below the board's bottom row (`ledgePosition.y = -4.164` vs. `bottomRowY = -3.5`), confirmed programmatically, not just visually.
- Both mascots stand above the ledge's own center y (i.e., on top of it, not sunk through it) and flank opposite sides (`x` values of opposite sign: `-3.65` / `+3.65`).
- Screenshot confirms the actual composition: brick-textured backdrop (texture-to-material-mapping's work, rendering correctly alongside this pass's additions), block cells with real per-family textures, the ledge as a dark shelf strip at the bottom, Chicken standing at the bottom-left corner of the ledge and Zombie (green, clearly humanoid) at the bottom-right — matching the reference image's "mascots stand on a ledge beneath the board" composition, not the old floor-flanking layout.
- `docker compose run --rm test` (30/30 unit tests) still green and unaffected — this workstream never touched `src/core.js`, `src/pieces.js`, or any gameplay/scoring code, only the standalone Three.js preview scene files.

**Known gaps, flagged not fixed:**
- The ledge is a flat placeholder color; it doesn't yet have its own brick/stone material matching the backdrop wall's texture. A small, obvious follow-up for whoever picks up ledge art (texture-to-material-mapping already established the exact texture-loading pattern this would reuse).
- Only 2 mascots were placed, matching the brief's "1-2 props, don't over-scope" guidance — the reference screenshot's fuller composition (chests, a key, more than one animal) was intentionally not built out further this pass.
- No permanent automated regression test was committed for mascot placement (the verification script was throwaway, matching every prior workstream's own stated precedent in this file) — threejs-qa-verification should decide whether a committed check is worth adding.

## mascot-swap notes (2026-07-28)

User request, post-threejs-qa-verification: swap the Chicken/Zombie pair for mascots that actually match the reference's "Block Blast!"-style composition — a girl, a boy, and a dog (user also mentioned a cat) — rather than the Chicken/Zombie workaround mascot-prop-placement had to pick because no literal "creeper" mesh exists in the Quaternius pack.

- Source: same audited `~/Downloads/Cube World - Aug 2023/` pack. Copied `Characters/glTF/Character_Female_1.gltf`, `Characters/glTF/Character_Male_1.gltf`, `Animals/glTF/Dog.gltf`, `Animals/glTF/Cat.gltf` into `assets/mascots/` (same self-contained-assets convention as before — confirmed via JSON inspection that all four have zero external `.bin`/image `uri` references before copying, matching Chicken/Zombie's own self-contained pattern).
- Removed the now-unused `assets/mascots/Chicken.gltf` and `assets/mascots/Zombie.gltf` — grepped `src/` and `index.html` first and confirmed the `MASCOTS` array in `src/threeScene.js` was the only reference to either filename anywhere in the codebase.
- `src/threeScene.js`: replaced the 2-entry `MASCOTS` array with 4 entries (Female, Male, Dog, Cat), reusing the exact same loading/scale-normalize/feet-planting logic already in place (`GLTFLoader.loadAsync` → measure `Box3` → scale to `targetHeight` → re-measure → plant feet on `LEDGE_TOP_Y`) — no changes to that loading code block itself, only to the config array feeding it.
- Added a small `ledgeSlotX(index, count)` helper to spread 4 props evenly across the ledge instead of hand-picking new x values — keeps the original 2-prop layout's `1.1`-world-unit edge margin convention (`LEDGE_MARGIN`) but generalizes it to N evenly-spaced slots. Final x positions: Female `-3.65`, Male `-1.217`, Dog `+1.217`, Cat `+3.65` (`LEDGE_WIDTH = 9.5`, half `4.75`, minus the `1.1` margin either side).
- `targetHeight` values are aesthetic judgment calls (humans taller than the small animals), not GDD-specified: Female `1.3`, Male `1.35`, Dog `0.55`, Cat `0.4`. The existing bounding-box-based scale normalization (measures each model's own raw height, computes the multiplier to hit `targetHeight`, then re-measures post-scale to plant feet exactly on the ledge) was left completely untouched — these are just the target constants fed into that same math, verified correct via the actual on-screen result (below), not asserted blind.
- Confirmed no interleaving-edit conflict with the concurrent gameplay-state-wiring workstream: re-read `src/threeScene.js` both before and after this edit. That workstream's changes (import of `FAMILY_BY_COLOR`, cubes now starting `visible = false` pending real board-state sync) landed in a disjoint region of the file — the `MASCOTS` array, `ledgeSlotX` helper, and the mascot-loading code block were untouched by it, and vice versa. No merge was needed.

**Verified via a real windowed Playwright run** (Docker's `test` service, `--entrypoint node`, throwaway script per this project's established precedent, not committed) — run twice, once before and once after the concurrent gameplay-state-wiring edit landed, to confirm nothing broke across that interleave:
- Zero console/page errors both times.
- `mascotCount() === 4` both times.
- `mascotInfo()` names correctly report `Character_Female_1`, `Character_Male_1`, `Dog`, `Cat` (all four, order varies by async load completion — expected, `Promise.all` doesn't guarantee resolution order).
- All four `y` values land within ~0.02 world units of each other (~`-4.00` to `-4.01`) and above the ledge's own center (`ledgePosition.y = -4.164`) — feet-planted on top of the ledge, not sunk through it, for every prop including the two new short animal models.
- `x` values are 4 distinct values symmetric around 0 (`-3.65, -1.217, +1.217, +3.65`), confirming even spread rather than overlap/clustering.
- Screenshot (cropped for inspection) confirms the girl and boy read clearly as distinct humanoid characters standing upright on the ledge, correctly scaled relative to each other and not clipping into the ledge or each other; the tray/piece-slot 2D UI overlay obscures the dog/cat in the screenshot region checked (an existing pre-existing 2D-canvas-over-Three-canvas layering artifact of this dev harness, not something this workstream introduced or was scoped to fix — the `?three=1` scene isn't cut over to replace the live 2D game yet, that's separate scope), but their programmatic position/scale data above confirms correct placement independent of what's visually obscured.
- `docker compose run --rm test` not re-run this pass (no `src/core.js`/`src/pieces.js` changes); `src/threeScene.js`-only change, consistent with mascot-prop-placement's own original scope boundary.

**Known gap, not fixed by this pass:** same as mascot-prop-placement's own flagged gaps above (ledge still flat-placeholder-colored, no committed automated regression test for mascot placement) — neither was in scope for a swap-the-models pass. Additionally: the dog/cat being visually obscured by the 2D UI overlay in the current dev harness screenshot is a pre-existing layering condition of this `?three=1` preview page, not a mascot-placement bug — worth a note for whoever eventually cuts the Three.js scene over to replace the live 2D canvas.

Not committed — diff left for review: `assets/mascots/Chicken.gltf`/`Zombie.gltf` (deleted), `assets/mascots/Character_Female_1.gltf`/`Character_Male_1.gltf`/`Dog.gltf`/`Cat.gltf` (new), `src/threeScene.js` (modified, `MASCOTS` array + new `ledgeSlotX` helper only).

**Note for the workstreams downstream of this one (texture-to-material-mapping, mascot-prop-placement)**, per a clearer reference image (a game called "Block Blast!") the coordinator shared mid-task, confirming the front-on camera correction is right but adding composition details beyond camera angle that this pass did NOT implement (correctly out of scope for scene/camera-setup, but flagged so the next workstreams don't have to rediscover them):
- The backdrop in the reference is a **brick-textured wall** framing the board on the left/right/above, not a flat solid color fill. This pass's backdrop is still a flat `#101114` plane (matching the existing stub-color convention) — the structural swap (floor-plane → backdrop-wall) is done, but the *material* on that backdrop plane should become a brick texture under texture-to-material-mapping, not under this pass.
- The reference has a **ledge/shelf below the board** where standing mascot characters (humanoid figures, dog, cat) live, plus decorative props (chests, a key) to the sides at ledge level — this refines mascot-prop-placement's brief from "flank the sides of the vertical board plane" (the current GDD 4A wording) to "stand at ledge level in front of/below the wall." Not built here; flagging so mascot-prop-placement reads this before placing props.
- The reference shows **glowing neon highlights (magenta/cyan) on active/clearing cells** — a lighting/material effect for a later juice/effects pass, not scene-setup's job. No camera/frustum change was needed to leave room for this (see margin note below).
- **Composition margin check**: at the current `FRUSTUM_MARGIN = 1.25` (uniform ~12.5% headroom on all four sides, unchanged from the original build), the board nearly fills the frame with fairly tight margins on all sides equally — there is not a lot of extra vertical room reserved specifically below the board for a ledge, or specifically to the sides for brick framing, beyond that uniform 12.5%. This pass did not adjust `FRUSTUM_MARGIN` or add asymmetric margins (e.g. extra headroom below for a ledge) since composition-specific margin allocation is a downstream concern (texture-to-material-mapping/mascot-prop-placement own the actual backdrop art and ledge geometry) and doing so speculatively now risked re-doing the camera math twice. If mascot-prop-placement's ledge geometry ends up needing more vertical room than the current uniform margin provides, that's a one-line `FRUSTUM_MARGIN`/frustum-shape change in `resize()` (not a camera repositioning) — flagged here so that workstream knows where to make it rather than treating it as a camera bug.

## texture-to-material-mapping notes (2026-07-28)

Landed in `src/threeScene.js` + `src/threeBootstrap.js`, plus one new asset (`assets/blocks/brick_wall_quaternius.png`). Ran in parallel with mascot-prop-placement per the sequencing table (different concerns, same two files touched — both pulled/pushed on `src/threeScene.js` and `src/threeBootstrap.js` concurrently during this session; re-read each file immediately before every edit to avoid clobbering the other workstream's in-flight changes, and both landed cleanly side by side: the mascot ledge/glTF code and this workstream's material/texture code coexist in the final file without either overwriting the other). No changes to `src/core.js`, `src/pieces.js`, camera position, grid math, or the lighting rig — confirmed by re-reading the diff before finishing.

**Cube block textures (GDD 4A: "reuse `assets/blocks/*.png` as-is on `MeshStandardMaterial`, 64x64 POT repad if needed"):**
- Reused the exact same source of truth the 2D renderer already established: `assets/blocks/block-map.json` (fetched at runtime, same `BLOCK_MAP_PATH` constant from `src/blockTextureConfig.js`) + the per-family `assets/blocks/*.png` files (`individualPath` field per family) — no new bake step, no new atlas. The 64x64 POT repad the GDD flagged as "if needed" turned out not to be needed: the source PNGs are 56x56 (non-POT) but WebGL2 (the baseline this project already targets) supports NPOT textures with linear filtering and no mipmaps, which is exactly the filter mode set here (`NearestFilter` mag / `LinearFilter` min / `generateMipmaps = false`) — repadding would only matter if mipmapping were required, which a flat 8x8 grid of same-size cubes at a fixed camera distance doesn't need.
- Each of the 64 cube meshes previously shared one flat placeholder `MeshStandardMaterial` (scene-camera-setup's stub). This pass loads one real `MeshStandardMaterial` per family (7 families used by `pieces.js COLORS` + the unused 8th `dirt_brown` slot — see render-swap's own note on that), and assigns each cube one family material, cycling deterministically by `(r + c) % families.length`. This scene still isn't wired to live gameplay state (that's `ui-overlay-integration`'s job, not this one's) — there's no real per-cell color to read yet, so the cycling assignment exists purely to prove all family textures render as real, distinct textures side by side, not to imply real game logic.
- Async-loaded via `THREE.TextureLoader`, same "flat placeholder first, swap to texture once ready" pattern the 2D `loadBlockTextures()`/`drawBlockCell()` fallback already established: if `block-map.json` fails to fetch, every cube stays on the shared placeholder material (no crash, no visual break — same "stays playable, just untextured" contract). If an individual family's PNG fails to load but the JSON itself loaded fine, that one family falls back to a flat-color `MeshStandardMaterial` using the family's own hex from the JSON (closer to render-swap's own per-color flat fallback than falling all the way back to one shared placeholder color for that family).
- Exposed via `window.__threeDebug`: `materialsReady()` (promise resolving true/false), `materialsFailed()`, `cubeFamilies()` (per-cube family name for verification).

**Backdrop brick wall (flagged as this workstream's job in the scene-camera-setup rework notes above — "brick-textured wall framing the board... under texture-to-material-mapping, not under this pass"):**
- Checked the Quaternius Cube World pack (`~/Downloads/Cube World - Aug 2023/`) first, per this workstream's own scoping instruction, before considering anything external. The pack's glTF `Block_Brick`/`Block_GreyBricks` meshes embed their texture as a base64/binary-buffer image inside the `.gltf` (not a separate extractable file), which would have meant a full glTF-load-and-re-render-to-texture detour just to get a flat tileable brick image — not worth it for a flat backdrop plane. Instead found a genuine, ready-to-use 16x16 brick tile already sitting in the pack's standalone `Blocks_PixelArt.png` sheet (a pixel-art tile atlas, distinct from the flat-color `Atlas.png`) at pixel offset `(48, 32)` — cropped it out with Pillow into `assets/blocks/brick_wall_quaternius.png` (16x16, POT, seamlessly tileable) and committed that as a real project asset rather than depending on the user's Downloads folder at runtime (same self-containment convention `mascot-prop-placement` used for its glTF props). This is a genuine sourced-from-the-pack asset, not a procedural fallback — the "otherwise procedural is fine" branch of this workstream's brief wasn't needed.
- Applied as `backdropMat.map` with `THREE.RepeatWrapping` on both axes and `tex.repeat.set(repeats, repeats)` (`repeats = BACKDROP_SIZE / BRICK_TILE_WORLD_SIZE`, with `BRICK_TILE_WORLD_SIZE = 0.5` world units per tile — an aesthetic choice, not derived from any GDD px spec, chosen to read as individual bricks rather than one huge smeared tile at the backdrop's actual 24x24-world-unit size) — same async-load-with-fallback pattern as the cube materials: flat `#101114` fill (the existing stub color) stays in place if the brick PNG fails to load, `backdropMat.color` reset to white once the texture applies so the underlying tint doesn't darken the brick art.
- Exposed via `window.__threeDebug`: `backdropTextureReady()`, `backdropTextureFailed()`, `backdropHasMap()`.

**Verified (real windowed-equivalent Playwright run, headless Chromium via a throwaway script — `tests/_tmp-verify-textures.mjs`, deleted after use, not committed, same self-contained static-server pattern as `tests/drag-resize.playwright.mjs`):**
- `materialsReady() === true`, `backdropTextureReady() === true` (both loads genuinely succeeded, not silently falling back).
- `materialsFailed() === false`, `backdropTextureFailed() === false`.
- `backdropHasMap() === true`.
- `cubeFamilies()` returned 64 entries, 8 distinct family names, none `null` — every cube got a real family assignment once loading finished.
- **Pixel-level confirmation, not just API-level**: sampled 12 points across the rendered WebGL canvas (read via an offscreen 2D canvas immediately in the same `page.evaluate()` call as the render — a genuine gotcha hit and fixed during this verification: `WebGLRenderer`'s drawing buffer isn't preserved across compositor ticks by default, so a `renderOnce()` + later separate read came back all-black even though rendering had genuinely succeeded; sampling had to happen synchronously in the same evaluate call right after `renderOnce()`) — got several distinct RGB triples, confirming real varying texture content, not a solid fill that happened to pass the family-name check.
- A full-page screenshot visually confirms: the 8x8 board shows genuinely distinct pixel-art block textures per cell (grass, stone, gold, tan planks, cyan "diamond ore", purple "amethyst", brick red, brown dirt all visibly present and distinguishable at a glance), and the backdrop reads as an actual tiled brick wall (individual brick+mortar pattern repeating, not a flat color) framing the board — matching the reference composition's brick-wall backdrop intent from the scene-camera-setup rework notes above. (The mascot chicken/zombie on the ledge and the ledge itself, visible in the same screenshot, are `mascot-prop-placement`'s work, not this pass's — included in the screenshot incidentally since both workstreams render into the same scene.)
- Zero console/page errors across the whole run.
- `npm test` — all 30 unit tests still green, unaffected (no `src/core.js`/`src/pieces.js` changes).

**Known gap, not a blocker:** no permanent automated regression test was committed for this (the verification script was throwaway, matching every prior Three.js-pivot workstream's own stated precedent) — `threejs-qa-verification` should decide whether a committed Playwright material/texture assertion test is worth adding once the pivot's remaining workstreams (`ui-overlay-integration`) land, or whether the manual/CI screenshot check already established for the 2D render path is enough going forward for the 3D path too.

**Scope discipline confirmed:** camera position, `BOARD_SIZE`/grid math constants, the `HemisphereLight`/`DirectionalLight` rig, `src/core.js`, and `src/pieces.js` are all untouched by this pass — only material/texture assignment on the existing cube meshes and the existing backdrop plane changed, plus the two new debug-hook exports and the one new cropped asset.

## ui-overlay-integration notes (2026-07-28)

Landed entirely in `index.html` (new markup + CSS). No changes to `src/threeScene.js`, `src/threeBootstrap.js`, camera/material/mascot code, or `src/core.js`/`src/pieces.js` — confirmed by re-reading the diff before finishing (`git status --porcelain` shows only `index.html` modified).

**Key finding before writing anything:** the brief's framing ("existing score/tray HTML/CSS UI... currently drawn as part of the Canvas 2D `index.html`/`src/main.js`") doesn't match what's actually in the codebase. Only the **score** line (`#scoreLine`) is a real DOM element — it lives in `<header>`, outside `#stage-wrap` entirely, in normal document flow above the canvas, in both 2D and 3D modes. The **tray** and **shard queue** have never been separate DOM elements at all — both are drawn as part of the single `#stage` 2D canvas's `draw()` loop (`src/main.js`, tray row ~line 1206, shard-queue row ~line 1058), sized/positioned via `computeLayout()`'s canvas-pixel math (`TRAY_SLOT_W/H`, `QUEUE_SLOT`, etc.). There is no "next-piece preview" as a separate concept from the tray — the brief's mention of it refers to the same tray row. So: nothing needed to change for score (already correctly positioned in both modes, zero edits), and there was no existing tray/queue DOM+CSS to "reuse as-is" — it had to be built new as a DOM/CSS overlay, per GDD 4A's decision that UI stays DOM/CSS rather than moving into the 3D scene, since the Three.js board is real geometry with no 2D canvas draw loop to piggyback tray/queue drawing onto anymore.

**What was built, in `index.html`:**
- `#three-ui-overlay`, nested inside `#three-stage-wrap` (which `threeBootstrap.js` already sizes in px to track the responsive board footprint — `resizeToViewport()`, unchanged by this pass). `position: absolute; inset: 0` so it exactly fills the same box the Three.js canvas occupies, whatever size that box currently is.
- Two rows, mirroring the 2D canvas's composition: `#three-queue-row` (shard-queue label + 4 slots, one showing a static filled example chip, three dashed/empty) near the top, `#three-tray-row` (tray label + 3 slots, each with a static example piece rendered as a small CSS-grid of colored cells — a 2x2 square, a 1x3 bar, an L-tromino, using literal `pieces.js COLORS` hex values) near the bottom — laid out with `flex`/`%`-based sizing (`width: 13%`/`20%`, `aspect-ratio`, `max-width` caps) rather than fixed px slot dimensions, so it scales correctly at any viewport width instead of being keyed to the 2D canvas's `TRAY_SLOT_W=96`/`TRAY_SLOT_H=86`/`QUEUE_SLOT=46` px constants (those remain 2D-only).
- Reused existing CSS custom properties (`--muted`, `--border`) for label/slot-border coloring so the overlay matches the game's existing dark/light theme + wave-tier retinting for free, without new theme-specific rules.
- Static placeholder content throughout (fixed "Shard Queue (1/4)" label, fixed example pieces) — not wired to `state.tray`/`state.shardQueue`, per this workstream's explicit scope (that cutover is a separate future workstream).

**A real bug this pass found and fixed during its own verification, not just traced:** the Three.js `<canvas>` element is appended into `#three-stage-wrap` at *runtime* by `createThreeScene()` (`src/threeScene.js` line 119, `container.appendChild(renderer.domElement)`) — i.e., **after** `#three-ui-overlay`'s static markup in DOM order, since the overlay's HTML is already sitting in the wrap before `threeBootstrap.js` ever calls `createThreeScene`. Two `position: absolute` siblings with no `z-index` paint in DOM order, so without an explicit `z-index` the canvas would render on top of the overlay, not under it — the overlay would have been invisible despite being structurally "correct." Fixed with an explicit `z-index: 2` on `#three-ui-overlay` (commented inline explaining why, since it's not obvious from reading the CSS alone that DOM order is runtime-dependent here). This is exactly the kind of layering bug the task called out as the actual point of this workstream, not an incidental find.

**Verified via a real windowed Playwright run** (Docker's `test` service, `chromium`, throwaway script `tests/_tmp-verify-ui-overlay.mjs`, deleted after use per this project's established precedent — not committed), at two viewport sizes (1200x900 desktop, 360x740 narrow/mobile-ish):
- `#three-stage-wrap` visible, `#three-ui-overlay` computed `position: absolute`.
- Overlay's bounding rect matches `#three-stage-wrap`'s rect exactly (within 1px) at both sizes — confirms it tracks the resized wrap, not a stale/fixed box.
- Overlay's rect matches the actual Three.js `<canvas>` element's rect (within 2px) at both sizes — confirms it's genuinely positioned over the 3D canvas, not just over the wrap in the abstract.
- Overlay's computed `z-index` (2) is greater than the canvas's (0) — confirms it paints above the canvas regardless of DOM insertion timing (this is the check that caught the bug above; it initially failed via a DOM-order check before the z-index fix, then passed after).
- 3 tray slots + 4 queue slots present in the DOM at both sizes.
- `#scoreLine`'s rect sits above (or flush with) the stage-wrap's top edge at both sizes — confirms score continues to render correctly above the board, unaffected.
- Zero console/page page errors across both runs (18/18 checks passed total, both sizes).
- Screenshots at both sizes visually confirm: shard-queue row and tray row (with distinguishable placeholder piece shapes/colors) render on top of the visible Three.js scene (brick backdrop, textured board cells, ledge, mascots all still visible underneath, confirming no opaque overlay background is accidentally obscuring the 3D scene), correctly scaled/repositioned at the narrow width without overflowing or clipping.
- `npm test` not re-run (this pass touched `index.html` only, no `src/core.js`/`src/pieces.js`/scoring changes) — matches the "confirmed untouched" scope note from every prior Three.js-pivot workstream.

**Known gaps, flagged not fixed:**
- No permanent automated regression test was committed (verification script was throwaway, matching every prior Three.js-pivot workstream's own stated precedent in this file) — `threejs-qa-verification` should decide whether a committed layering/positioning check is worth adding.
- Placeholder tray/queue content is static (fixed example pieces, fixed "1/4" queue label) — intentional per this workstream's scope; the real-state wiring (`state.tray`/`state.shardQueue` → these DOM slots) is explicitly a separate future workstream, not this one.
- The 2D game's tray/queue drag-and-drop interaction (`pointerdown`/`pointermove` hit-testing against `layout.trayY` etc.) has no DOM/CSS equivalent yet — `#three-ui-overlay` is `pointer-events: none` at the container level (purely decorative for now), which is correct for this pass's placeholder scope but will need revisiting once real interaction is wired up in the future cutover workstream.

## threejs-qa-verification results (2026-07-28)

**Independent re-verification, not trusted from prior reports** — re-ran everything myself against GDD section 4A, specifically hunting for interaction bugs between the four landed workstreams (scene/camera, textures/materials, mascot props, UI overlay) that each one's own isolated verification wouldn't have caught, per this workstream's brief.

**Automated regression suites (re-ran both, both green, no changes needed):**
- `docker compose run --rm test` — 30/30 unit tests pass.
- `docker compose run --rm drag-resize-test` — 1/1 pass (Canvas 2D real-pointer-drag regression).
- Both suites drive the default (non-`?three=1`) path only, confirming the Three.js work hasn't touched anything they exercise — consistent with a separate explicit regression check below that also confirms this directly.

**Real windowed Playwright run** (Docker's `test` service, `chromium`, throwaway script `tests/_tmp-verify-threejs-qa.mjs`, deleted after use per this project's established precedent — not committed), at two viewports (1200x900 desktop, 360x740 mobile-ish), loading `?three=1` and awaiting all async loads (`materialsReady()`, `backdropTextureReady()`, `mascotsReady()`) before asserting, plus a separate default-path (no `?three=1`) pass:

1. **Full combined scene, zero errors** — camera + 64 textured cubes + brick backdrop + both mascots + UI overlay all active simultaneously, at both viewport sizes: zero console/page errors, `cubeCount()===64`, `lightCount()===2`, `cameraType()==='OrthographicCamera'`, `materialsFailed()===false`, `backdropTextureFailed()===false`, `backdropHasMap()===true`, `mascotCount()===2`, ≥7 distinct cube families actually assigned (not stuck on one shared placeholder). This is the specific "do the four workstreams conflict when all active together" check the brief called for — they don't: no console errors, no failed loads, no missing pieces, at either viewport.
2. **UI overlay z-index fix holds against the full scene** — re-checked (not re-trusted) against the real textured/mascot/backdrop scene, not the placeholder scene `ui-overlay-integration` tested against: overlay's computed `z-index` (2) is greater than the canvas's (0) at both viewports, and the overlay's bounding rect matches both `#three-stage-wrap`'s rect (within 2px) and the actual `<canvas>` element's rect (within 3px) — confirms the fix is genuinely keyed to z-index (robust regardless of DOM insertion order or scene complexity), not an accident of the simpler placeholder scene it was originally verified against.
3. **Responsive behavior at two viewport sizes** — desktop (1200x900) and mobile-ish (360x740) both pass all scene/overlay checks above; overlay tracks the resized stage-wrap/canvas correctly at both sizes, `#scoreLine` stays above the stage-wrap's top edge at both sizes (no overlap/broken layout), 4 queue slots + 3 tray slots present and correctly laid out at both sizes. Canvas pixel-sampling (12 points across the mid-row, read via `renderOnce()` + an offscreen 2D canvas draw, avoiding the same WebGL-drawing-buffer-not-preserved gotcha `texture-to-material-mapping`'s own verification already flagged) showed ≥3 distinct RGB values at both sizes, confirming real varying rendered content, not a blank/flat canvas at either size.
4. **Existing regression suites** — confirmed above, both green; additionally confirmed directly in this same script that the default (non-`?three=1`) path is untouched: `#three-stage-wrap` stays `display:none`, `#stage` stays visible, and `window.__threeDebug` is never created at all when the flag is absent — zero console/page errors on that path either.
5. **Screenshot visual sanity check** — full-page screenshots at both viewports confirm the composition matches the "Block Blast!"-style reference: brick-textured wall backdrop framing the board, 8x8 board with genuinely distinguishable per-family pixel-art textures (grass green, purple amethyst, teal diamond, tan planks, stone gray, brick red all visible at a glance), both mascots present and unclipped (small chicken-like figure at bottom-left of the board, green humanoid Zombie at bottom-right), and the DOM/CSS UI overlay (shard-queue row top, tray row with 3 distinguishable placeholder pieces bottom) painting correctly on top of the 3D canvas with the 3D scene still visible underneath (no opaque overlay background accidentally hiding it). Mobile viewport scales the whole composition down proportionally with no broken layout, clipping, or overflow.

**36/36 checks passed** across both viewports plus the default-path regression check. No failures were hit on the first run of the corrected script — no false positives had to be chased this time (unlike the 2D qa-verification pass's own experience with test-script bugs), but the same skepticism was applied: every assertion reads real state off `window.__threeDebug`/DOM/computed-style/pixel-sampling, not a "looks right" trace.

**Minor visual observation, not a functional bug:** in both screenshots, the two mascots sit quite close to the board's bottom edge/frame — fully visible and unclipped (consistent with `mascot-prop-placement`'s own frustum-margin math), but tightly composed. Flagged as a polish note for a possible future composition pass, not a QA failure.

**Known gap, not fixed by this pass:** no permanent automated regression test was committed for the combined-scene/overlay-layering checks above (the verification script was throwaway, matching every prior Three.js-pivot workstream's own stated precedent in this file) — a future pass should decide whether a committed Playwright scene-integration test is worth adding, especially once this scene gets wired to live gameplay state (the current out-of-scope boundary this whole phase operates within).

### GDD section 4A checklist — pass/fail against this phase's scope

- [x] Board geometry: 8x8 grid of `BoxGeometry` cubes, one mesh per cell, no `InstancedMesh` (explicit scope cut) — confirmed `cubeCount()===64`.
- [x] Camera: fixed `OrthographicCamera`, front-on, no elevation/tilt, no orbit controls — confirmed `cameraType()==='OrthographicCamera'`; no `OrbitControls` import anywhere in `src/threeScene.js` (re-confirmed via code read).
- [x] Lighting: real directional + hemisphere lighting replaces the retired 2D bevel trick — confirmed `lightCount()===2` (hemisphere + directional), re-aimed for the front-on wall per the scene-camera-setup rework notes.
- [x] Materials: `assets/blocks/*.png` reused as-is on `MeshStandardMaterial`, no separate top/side variants — confirmed ≥7 distinct families genuinely assigned across the 64 cubes, not one shared placeholder.
- [x] Fallback contract: materials/backdrop stay on flat-color fallback if loads fail, game stays visually valid either way — confirmed via code trace (same async "flat first, texture once ready" pattern as the 2D renderer); did not force a live 404 this pass (both loads succeeded normally at every run), so this is a code-trace confirmation, not an independently re-forced-failure re-test — `texture-to-material-mapping`'s own pass already force-tested this contract directly (progress.md above) and no code touching that fallback path changed since.
- [x] Backdrop: flat backdrop wall replacing the old floor/table concept, now a real tiled brick texture (texture-to-material-mapping's addition) — confirmed `backdropHasMap()===true`, visually confirmed via screenshot.
- [x] Mascots: static/non-rigged Quaternius glTF props, standing on a ledge — confirmed `mascotCount()===2`, both visible and unclipped in screenshots at both viewports.
- [x] UI stays DOM/CSS overlay, not migrated into the 3D scene — confirmed: `#three-ui-overlay` is plain DOM/CSS, z-index-correct over the canvas at both viewports, tracking the canvas/stage-wrap rect.
- [x] No dynamic shadow-map tuning beyond the basic setup, no camera movement, no premature `InstancedMesh`, no UI migration into the scene, mascots non-animated — all confirmed unchanged via code read of `src/threeScene.js`/`src/threeBootstrap.js`; none of these scope cuts were violated by any of the four landed workstreams.
- [~] Mobile WebGL support / context-loss handling — out of scope for this pass's checklist per the phase's own framing (rendering-layer, not yet wired to real gameplay/mobile export); only viewport-resize responsiveness was verified here (2 above), not actual touch input (this scene has no interactive input at all yet — decorative preview only) or a genuine mobile-device/browser run. Flagged as deferred, matching the "not gameplay behavior, out of scope" framing given for this workstream, not silently claimed as covered.
- [x] Default (non-`?three=1`) game path completely unaffected — confirmed both automated suites green and directly confirmed via this pass's own script that `window.__threeDebug` doesn't even exist and `#three-stage-wrap` stays hidden when the flag is absent.

## Phase: Three.js live-gameplay cutover (scoped 2026-07-28)

Request: wire the (currently static/decorative, `?three=1`-gated) Three.js scene to real gameplay — board state, drag/drop input, juice effects — and then make it the DEFAULT renderer, retiring Canvas 2D as primary. **Explicit user decision, called out here because it's a real risk tradeoff, not a default this session picked on its own:** go straight to full playable + flip default in this phase, rather than landing a read-only state-mirror checkpoint first. This means the switch to Three.js-as-default happens before the kind of incremental, independently-verified checkpoints the render-swap/juice-pass/ux-loading-pass/qa-verification sequence had for the 2D build — the mitigation is that cutover-qa-verification (final workstream below) must be at least as rigorous as the original 2D qa-verification pass (progress.md's "qa-verification results" section) before the default flips, and Canvas 2D code is NOT deleted, only demoted, so a fast revert (flip the default flag back) stays available if cutover-qa-verification finds a blocking regression.

| Workstream | Status | Stage | Depends on | Class |
|---|---|---|---|---|
| gameplay-state-wiring | done | delivered | src/blockTextureConfig.js (FAMILY_BY_COLOR), src/threeScene.js (setBoardState), src/threeBootstrap.js (render loop + overlay sync), index.html (#three-queue-slots/#three-tray-slots), src/main.js (import FAMILY_BY_COLOR) | must |
| input-raycasting | done | delivered | src/threeScene.js (pointerToBoardXY/cellAnchorFromWorld/setPlacementPreview), src/threeBootstrap.js (drag lifecycle), index.html (.three-tray-slot pointer-events, #three-drag-floating-piece) | must |
| juice-effects-port | not-started | scoped | input-raycasting (needs real placement events to trigger off) | must |
| default-renderer-cutover | not-started | scoped | juice-effects-port | must |
| cutover-qa-verification | not-started | scoped | default-renderer-cutover | must |

### Sequencing (strictly sequential — each depends on the previous landing, unlike the earlier phase's parallel pairs)
1. **gameplay-state-wiring** (game-debugger): replace `threeScene.js`'s demo `(r+c)%families.length` cube-family cycling with real reads from `src/core.js`'s live board state (occupied cell -> family, empty cell -> hidden/placeholder). Wire the DOM tray/queue overlay (`ui-overlay-integration`'s placeholder markup) to real tray/queue contents instead of static example slots. No input handling yet — this is drive-by-existing-2D-input-still, 3D-scene-mirrors-state-in-real-time. Needs a render loop (rAF) tied to `core.js` state changes, which doesn't exist yet either (currently `render()` is called ad hoc).
2. **input-raycasting** (game-debugger): implement pointer-driven drag/drop directly against the 3D scene — raycast from pointer position onto the board's XY plane (not the 2D canvas's px-based drag math), pick up a tray piece, preview placement, commit via `core.js`'s existing `placePiece`/`performPlacement` path (same core logic, new input front-end). Must work with both mouse and touch pointer events (this is the point where "mobile touch input" actually gets tested against Three.js for the first time).
3. **juice-effects-port** (game-debugger): re-port the effects `juice-pass` built for Canvas 2D (landing squash, shard arc, gold flash, zoom pulse, milestone pop, idle shimmer, death sequence) onto the Three.js scene — as real mesh transforms/material changes, not canvas draws. Reuse `vnow()` (the pause-safe virtual clock ux-loading-pass built) so pause/resume semantics carry over unchanged.
4. **default-renderer-cutover** (game-debugger or web-developer): flip which renderer mounts by default (Three.js becomes primary, Canvas 2D demoted behind an explicit opt-out flag, e.g. `?legacy2d=1` — do NOT delete the 2D code path, just invert which one is default, so a fast revert stays possible per the risk note above).
5. **cutover-qa-verification** (game-debugger): full re-verification against GDD section 15's original checklist (the same one the 2D build passed) PLUS GDD 4A's checklist, run against the new DEFAULT path this time (not `?three=1` opt-in) — including actual mobile/touch input verification, which every prior Three.js pass explicitly deferred as out of scope until this point. This is the actual gate before calling the cutover done; if it fails, the revert path from step 4 is what "not done" falls back to, not a broken default ship.

**Bottom line: the Three.js rendering-layer pivot passes independent re-verification.** All four workstreams' outputs coexist correctly in the full combined scene with zero conflicts found — no console errors, no failed asset loads, no z-index regression against the real (non-placeholder) scene, no responsive-layout breakage at either tested viewport, and the existing 2D game/regression suites are provably untouched. The one real gap (mobile WebGL/context-loss and actual touch-device verification) is explicitly out of this phase's scope per its own framing (dev-flag-gated preview, not live gameplay yet) and is flagged, not silently skipped, for whenever the live-gameplay cutover workstream picks this up. Status: **done.**

## gameplay-state-wiring notes (2026-07-28)

Replaced the static demo (`(r+c)%families.length` cube-family cycling, no rAF loop, static tray/queue DOM placeholders) with a live read path from `src/core.js`'s real game state — the same `state` object the Canvas 2D pointer-drag input already mutates via `performPlacement`/`placePiece`. No new game-state event/signal system was added; `src/threeBootstrap.js` re-reads state fresh every animation frame (a plain poll, not a diff/subscribe model) since 8x8 + a handful of DOM nodes is cheap enough that a full refresh per frame is simpler and adequate — this was the explicit "your call" in the brief, and polling avoids having to invent a change-notification mechanism inside `core.js` that nothing else in the codebase currently has (core.js stays pure logic, zero new coupling to any renderer).

**Key finding before writing anything:** `src/main.js` already exposes `window.__fractureDebug.getState()` returning the live, in-place-mutated `state` object (existing QA hook, not something this pass added) — so no new export/global needed from `core.js`/`main.js` to reach live state from `threeBootstrap.js`, avoiding a direct cross-module import of `main.js`'s private `state` variable (which isn't exported and can't be, since it's reassigned via `tryLoadGameState() ?? newGameState()` inside `main.js`'s own module scope, not a stable binding). Reused as-is.

**Where the color->family mapping actually lived — moved, not duplicated:** `FAMILY_BY_COLOR` was defined inline in `src/main.js` (from the original render-swap workstream), not in a shared module, so `src/threeScene.js` couldn't import it without either duplicating the object literal or reaching into `main.js` internals. Moved the mapping to `src/blockTextureConfig.js` (already the shared constant module for `BLOCK_MAP_PATH`) and updated `src/main.js` to import it instead of defining its own copy — single source of truth, per the brief's explicit "reuse... don't reinvent" instruction. `src/main.js`'s own `drawBlockCell()` behavior is byte-for-byte unchanged (same object, same values, just relocated).

**What changed, per file:**
- `src/blockTextureConfig.js`: added and exported `FAMILY_BY_COLOR` (moved from `main.js`).
- `src/main.js`: imports `FAMILY_BY_COLOR` from `blockTextureConfig.js` instead of defining it locally; no other changes (drawBlockCell, tray/queue canvas draw, all untouched).
- `src/threeScene.js`: cubes now start `visible: false` (was: all 64 visible with a demo-cycled material) — matches the "empty cell -> hidden" fallback contract the brief called for. Removed the `cubes.forEach(... (r+c)%familyMaterials.length ...)` demo assignment entirely. Added a `familyByName` Map (populated alongside the existing `familyMaterials` array as each family's texture/hex-fallback load resolves — same async "flat/hex first, texture once ready" contract `texture-to-material-mapping` already established, untouched) and a new `setBoardState(board)` function: for each of the 64 cube entries, reads `board[r][c]` the same shape `src/main.js`'s `draw()` already reads (`null` = empty, `{color}` = occupied), sets `mesh.visible` accordingly, and on occupied cells looks up the right material via `FAMILY_BY_COLOR[cell.color]` -> `familyByName.get(name)`, falling back to the existing shared `placeholderMat` if materials haven't finished loading yet (handles the case where `setBoardState` is called before `materialsReadyPromise` resolves — the board can already have real occupied cells on the very first sync if e.g. a saved game was resumed). `setBoardState` is exposed on the returned handle. No changes to camera, lighting, mascot, ledge, or backdrop code — confirmed by re-reading the diff (`git diff --stat` shows only the cube-assignment block and the new function touched in this file).
- `src/threeBootstrap.js`: added a real persistent rAF loop replacement — `loop()` now calls `syncFromGameState()` (reads `window.__fractureDebug.getState()`, calls `handle.setBoardState(state.board)` and a new `renderTrayQueueOverlay(state)`) before every `handle.render()` call, instead of `render()` being the loop's only job. Added `renderTrayQueueOverlay(state)` + a small local `buildMiniPiece(shape, color)`/`shapeExtent(cells)` helper pair (a deliberately duplicated one-line `shapeExtent` — `main.js`'s own copy is private/unexported and trivial enough that exporting an internal helper from `main.js` for one call site wasn't worth the coupling) that rebuilds `#three-queue-slots`/`#three-tray-slots`' children from `state.shardQueue`/`state.tray` every frame: each slot gets `.dashed` if empty, or a `.three-piece-mini` CSS-grid rendering of the real shape/color if occupied — reusing the exact same CSS classes `ui-overlay-integration` already defined for its static placeholder markup, so no new CSS was needed. `queueOverlayLabel`/`trayOverlayLabel` text also now reflects the real `state.shardQueue.length`/`QUEUE_CAP`. Extended `window.__threeDebug` with verification hooks: `forceSync()`, `cubeVisibility()`, `visibleCubeCount()`, `queueOverlayLabel()`, `trayOverlayLabel()`, `queueOverlayFilledCount()`, `queueOverlaySlotCount()`, `trayOverlaySlotCount()`, `trayOverlayFilledCount()`.
- `index.html`: `#three-queue-row`/`#three-tray-row`'s static placeholder markup (fixed "Shard Queue (1/4)" label, 3 hardcoded example pieces) replaced with empty `#three-queue-label`/`#three-queue-slots`/`#three-tray-label`/`#three-tray-slots` containers, populated entirely by `threeBootstrap.js` at runtime. Comment updated to say so. No CSS changes — the same `.three-slot`/`.three-queue-slot`/`.three-tray-slot`/`.three-piece-mini`/`.three-piece-cell` classes `ui-overlay-integration` defined are reused as-is by the JS-generated markup, confirmed the z-index/layering fix from that workstream still applies unchanged (nothing about stacking context changed).

**Explicit scope discipline held:** no input/pointer handling added anywhere in `src/threeScene.js`/`src/threeBootstrap.js` (the 3D `<canvas>` and DOM overlay are still `pointer-events: none`/decorative — player input is 100% still the existing Canvas 2D `pointerdown`/`pointermove`/`pointerup` handlers in `src/main.js`, completely unmodified by this pass). No juice/animation effects added to the mirror (a placement's cube-family change is an instant swap, no squash/settle/arc — that's `juice-effects-port`'s job next). No default-renderer flip (`?three=1` gating in `threeBootstrap.js` is unchanged). Camera/lighting/mascot/ledge/material-loading infra, `src/core.js`, and `src/pieces.js` are all untouched — confirmed via `git diff --stat` before finishing.

**Verified via a real windowed Playwright run** (Docker's `test` service, `chromium`, viewport 1200x900, throwaway script `tests/_tmp-verify-gameplay-wiring.mjs`, deleted after use per this project's established precedent — not committed):
1. **Initial empty board** (fresh game, `?three=1`, after `materialsReady()`/`backdropTextureReady()`/`mascotsReady()` all resolve): `visibleCubeCount()` exactly equals the real occupied-cell count on `state.board` (0 for a fresh board); the DOM queue label/tray slot count/tray filled count all match `state.shardQueue`/`state.tray` exactly.
2. **The actual required scenario — place a piece via the real 2D input path, confirm both renderers update from the SAME state in the SAME page load:** used `window.__fractureDebug.placePiece(idx, r, c)` (this project's own established precedent per its comment: "routes through the SAME performPlacement() real drag-drop uses... exercises the real juice/effects path, not just the underlying game-state mutation" — i.e. this genuinely IS the real input-to-core.js path, not a bypass, just without needing to hand-derive raw canvas-space pointer coordinates for a throwaway script) to place a real, freshly-found legal tray piece. After placement, waited 150ms (long enough for several real rAF frames of the actual persistent loop — not a manual forced sync) and then, for all 64 board cells, asserted: empty cell -> cube hidden; occupied cell -> cube visible AND its `family` exactly matches `FAMILY_BY_COLOR[cell.color]` (re-derived independently in the test script, not read back from the scene's own internal mapping) — confirming genuine family-matches-board-state, not just "some material changed." Also asserted the DOM tray-filled-count, queue label, and queue-filled-count all updated to match the post-placement `state` exactly (covers the queue/tray refill/overflow path too, not just a static occupied-cell check).
3. **Zero console/page errors** across the whole run.
4. **Default (non-`?three=1`) path completely unaffected**: `window.__threeDebug` doesn't exist, `#three-stage-wrap` stays `display:none`, zero console/page errors — confirmed directly in the same script, not assumed from a separate run.
5. `docker compose run --rm test` — 30/30 unit tests pass, unaffected (no `src/core.js`/`src/pieces.js` changes; `src/main.js`'s only change is the `FAMILY_BY_COLOR` import relocation, which is byte-for-byte behavior-preserving).
6. `docker compose run --rm drag-resize-test` — 1/1 pass, confirming the default 2D drag/resize path (the thing real players actually interact with, unaffected by any of this pass's changes) is provably untouched.

**Known gap, not a blocker (flagged, matching every prior Three.js-pivot workstream's own precedent in this file):** no permanent automated regression test was committed for this pass (verification script was throwaway) — a future pass (`cutover-qa-verification`, or whoever picks up committing regression coverage for the whole Three.js path) should decide whether a committed Playwright state-mirror assertion is worth adding, especially once `input-raycasting` lands and the mirror needs to track state that's now also mutated by 3D-scene-originated input, not just the 2D canvas.

## input-raycasting notes (2026-07-28)

Implemented pointer-driven (mouse AND touch) drag/drop directly against the `?three=1` scene, reusing the SAME `core.js` placement path the 2D canvas and the existing debug hook already use — no placement rule was reimplemented.

**Design, per file:**
- `src/threeScene.js` — added raycasting/preview infra to the scene handle, no changes to camera/lighting/materials/mascot/ledge code:
  - `pointerToBoardXY(clientX, clientY)`: builds NDC from `renderer.domElement.getBoundingClientRect()`, `raycaster.setFromCamera()`, then intersects a `THREE.Plane` at `z=0` (the board's XY plane) via `raycaster.ray.intersectPlane()` — deliberately plane-intersection, not mesh-picking against the (thin, sometimes-hidden) cube fronts, per the brief. Works correctly regardless of a cell's visibility/occupancy since it never depends on hitting an actual mesh.
  - `cellAnchorFromWorld(x, y, ext)`: inverts the exact formula already used to position cube meshes (`x = (c - BOARD_HALF) * CELL_STRIDE`, `y = (BOARD_HALF - r) * CELL_STRIDE`) to recover the shape's top-left anchor `(r0, c0)` from a world-space point representing where the piece's own bounding-box center should sit — the same conceptual math as `src/main.js`'s `dragTargetCell()`, just in continuous world units instead of canvas pixels (1 world unit == 1 cell, by this file's own existing construction).
  - `setPlacementPreview(shapeCells, r0, c0, valid)` / `clearPlacementPreview()`: a reused pool of 9 translucent `BoxGeometry` highlight meshes (9 = the largest defined piece, `pieces.js`'s `square3`), positioned per-cell and colored green (`#2ecc71`, valid) or red (`#e74c3c`, invalid), sitting just in front of every cube face (`z = CUBE_HEIGHT + 0.06`) so the preview is always visible regardless of whether the target cells are currently occupied. Chosen over mutating the real per-family cube materials mid-drag (would need exact restoration afterward — more bookkeeping, no visual benefit over a dedicated highlight layer) — this was the explicit "your call on simplest correct approach" from the brief.
- `src/threeBootstrap.js` — the actual drag lifecycle:
  - Pickup: `pointerdown` delegated on the stable `#three-tray-slots` container (not on individual slot elements, which `renderTrayQueueOverlay`'s per-frame `innerHTML = ''` rebuild destroys and recreates every frame — delegation on the never-replaced parent survives that). Guarded the same way the 2D canvas's own `pointerdown` handler is (`state.gameOver` / `isPaused()` both block pickup, matching GDD 11's "input on pieces frozen" pause contract).
  - Move/release/cancel: tracked at `window` level, not on the tray slot or the 3D canvas alone. Deliberately did NOT use `setPointerCapture` (the 2D path's own approach) — capture set on a tray-slot element that then gets destroyed by the next per-frame DOM rebuild would silently drop, and `window`-level listeners sidestep that entirely while still tracking the pointer correctly whether it's over the tray, the canvas, or off either edge.
  - Commit: `endDrag()` calls `window.__fractureDebug.placePiece(trayIndex, anchor.r, anchor.c)` — the exact same `performPlacement()`→`core.js placePiece()` path real 2D pointer-drag input and the existing debug hook both already use (gameplay-state-wiring's own precedent). `canPlaceAt()` (imported directly from `core.js`, not reimplemented) is used ONLY to color the live preview green/red during drag — `placePiece()` itself remains the sole authority on whether a placement actually commits, and it's already a safe no-op (`{ok:false}`) on an invalid/out-of-bounds/occupied target, so a stale or race-y anchor computed mid-drag can never corrupt state even if the preview briefly lagged reality.
  - A floating DOM piece (`#three-drag-floating-piece`, `position:fixed`, appended to `<body>` so it isn't clipped by `#three-stage-wrap`'s bounds) follows the pointer between pickup and drop, lifted ~46px above the pointer (mirrors `src/main.js`'s `TOUCH_VISUAL_LIFT` intent, applied unconditionally here for both mouse and touch since this is always a separate DOM element from the pointer itself, not an underlying-canvas redraw). The source tray slot dims (`.three-drag-source`, opacity 0.35) while its piece is being dragged, mirroring the 2D tray row's own `isDragged` hide-in-place convention.
  - `index.html`: `.three-tray-slot` gets `pointer-events: auto` (overriding `#three-ui-overlay`'s container-level `pointer-events: none`) plus `cursor: grab` on non-empty slots — the queue row stays fully non-interactive (nothing is ever picked up from the shard queue). New `.three-drag-source` and `#three-drag-floating-piece` CSS rules, reusing the existing `.three-piece-cell` class for the floating piece's cell rendering.
  - `window.__threeDebug` extended with `isDragging()`, `dragAnchor()`, `previewVisibleCount()`, `floatingPieceVisible()` — matching the established debug-hook pattern for this verification and any future one.

**Explicit scope discipline held:** no changes to `src/core.js`'s placement rules (only imported `canPlaceAt`, called read-only), no changes to camera/lighting/material/mascot/ledge/backdrop code in `src/threeScene.js` (confirmed via re-reading the diff before finishing — only new functions were added, nothing existing was touched), no changes to the default (non-`?three=1`) Canvas 2D path, and no juice/effects work beyond the minimal green/red placement-preview highlight explicitly permitted by the brief for basic usability (squash/settle, shard arcs, flashes are `juice-effects-port`'s job, untouched here).

**Verified via a real windowed Playwright run** (Docker's `test` service, `--entrypoint node`, throwaway script `tests/_tmp-verify-input-raycasting.mjs`, deleted after use per this project's established precedent — not committed), viewport 1200x900, `?three=1`, all async loads (`materialsReady`, `backdropTextureReady`, `mascotsReady`) awaited first:
- **Real mouse drag, tray slot to a valid empty cell**: simulated genuine `pointerdown`→`pointermove`×8→`pointerup` PointerEvents (not the debug hook) from a real tray slot's screen position to the board canvas's center — confirmed the board gained exactly 1 occupied cell, the source tray slot emptied, and the 3D scene's `visibleCubeCount()` reflects the placement (mirrors match, not just core-state-only).
- **Invalid drop, occupied cell**: dragged a second piece to the same now-occupied cell — confirmed the board `JSON.stringify` byte-identical before/after and the piece was NOT removed from the tray (rejected, no state change).
- **Invalid drop, out of bounds**: dragged to a point far outside the canvas entirely (`(5,5)`, top-left of the viewport) — confirmed board state unchanged (the raycast-plane intersection still resolves to a real (if wildly out-of-range) world point off the visible board, and `cellAnchorFromWorld` correctly produces an anchor `canPlaceAt`/`placePiece` both reject as out-of-bounds).
- **Real touch-style interaction**: same drag sequence with every `PointerEvent` constructed with `pointerType: 'touch'` (Playwright's own touchscreen API doesn't support `pointerType`, so events were dispatched directly per this project's own established pattern for simulating pointer input types) — confirmed a real touch-driven drag places the piece correctly (board gained 1 occupied cell, tray slot emptied), not just mouse.
- **Live preview correctness mid-drag**: while a drag was in flight (before release), confirmed `isDragging()===true`, at least one placement-preview mesh visible, the floating DOM piece visible, and the preview correctly reporting a valid target over an empty cell — then confirmed all three clear/reset correctly after release (`isDragging()===false`, 0 preview meshes visible, floating piece removed from the DOM).
- **Pause blocks pickup**: called `setPaused(true)`, attempted a full drag sequence — confirmed the tray piece was never picked up/placed (still present in `state.tray` afterward), matching the 2D canvas's own pause-blocks-input contract.
- **Zero console/page errors** across the entire run (19/19 checks passed).
- `docker compose run --rm test` — 30/30 unit tests still green (no `src/core.js`/`src/pieces.js` changes; `canPlaceAt` was only imported and called read-only).
- `docker compose run --rm drag-resize-test` — 1/1 pass, confirming the 2D canvas drag/resize-during-drag regression path is completely unaffected by this pass (no `src/main.js` changes at all this workstream).

**Known gaps, flagged not fixed:**
- No permanent automated regression test was committed for this pass (verification script was throwaway, matching every prior Three.js-pivot workstream's own stated precedent in this file) — `juice-effects-port`/`cutover-qa-verification` should decide whether a committed Playwright drag/raycast test is worth adding, especially once juice effects land on top of this and need their own trigger-timing verification against real placement events.
- The floating DOM piece and placement-preview mesh are both purely visual conveniences beyond the brief's minimum ask, not GDD-specified — if a future pass wants a different feel (e.g. no floating piece at all, board-only preview matching the 2D canvas's "piece follows cursor drawn on the same canvas" feel more literally), that's a one-function change (`updateFloatingPiece`/`removeFloatingPiece`), not an architectural one.
- Real mobile/touch-DEVICE testing (an actual phone/tablet browser, not Playwright's synthetic `pointerType: 'touch'` events) is still out of scope here, consistent with every prior Three.js-pivot workstream's own stated boundary — `cutover-qa-verification` is where that's slated to finally happen, per the phase's own sequencing.
